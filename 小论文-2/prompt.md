你现在接手 `/home/kent/apollo-miku-planner` 中“二区版 MIKU 论文”的实质性升级任务。

  请先创建一个持续 Goal，然后直接开始执行，不要停留在计划、建议或语言润色层面。目标不是保证录用，而是通过算法、理论、实验和论文表达的共同升级，使稿件真正具备投稿 T-IV、T-ITS、RA-L 或同层次期刊的竞争力。如果证据表明现有创新不足，必须修改算法并重新实验，不能靠改标
  题、重新命名已有模块或夸张措辞包装创新。

  一、上下文与文件边界

  请先完整阅读以下两个历史会话：

  - `01a0683d-2618-7e42-b157-9681429df75a`
  - `01a06846-e9f8-7071-9324-be3b495829f0`

  但必须以当前仓库、代码、数据和 PDF 为权威状态，不得仅凭历史结论判断完成。

  稿件边界：

  - `小论文/`：导师要求停止技术修改的三区/四区版本。只允许读取和进行重合度审计，禁止修改其中任何文件。
  - `小论文-2/`：本次需要继续夯实的二区版本，可以修改算法、代码、实验、图表、中英文论文及复现材料。
  - 如果必须修改两稿共用的代码，要确保旧行为能够通过版本化配置或兼容接口复现，不能悄悄破坏第一稿的证据。
  - 修改前记录 Git 状态和基线提交，保护用户已有改动；使用逻辑清晰的 Conventional Commits。

  还要建立两稿重合矩阵。若二区稿最终仍只是第一稿的改写或增强版本，必须明确指出它不能与第一稿同时投稿；不得通过改标题、方法名或作者顺序掩盖重合。

  二、采用“裁判—审稿人—运动员”交叉审阅机制

  主代理担任 Area Chair/裁判和最终集成者，并明确创建三个并行子代理：

  1. `algorithm_athlete`
     - 作为算法研究者和实现者；
     - 负责寻找真正可成立的核心创新；
     - 修改二区版代码、理论、测试和实验；
     - 不得捏造结果，不得把 Apollo 已有模块重新命名成原创算法。

  2. `novelty_reviewer`
     - 作为 T-IV/T-ITS/RA-L 的敌意方法审稿人；
     - 第一轮只读、独立审稿，不看运动员的自我评价；
     - 搜索并阅读最新、最相关的原始论文，重点比较路径—速度协调、时空安全走廊、空间/时间同伦、K-best 候选、鲁棒占据和滚动规划；
     - 对每项“创新”判断：已有方法、自然组合、工程实现，还是可独立成立的新贡献；
     - 必须给出最可能导致拒稿的三至五个问题及相关先行工作。

  3. `evidence_reviewer`
     - 作为实验、理论正确性和论文表达审稿人；
     - 检查定理是否只是显然的区间排序结论，证明条件是否覆盖真实算法；
     - 检查实验是否公平、是否真正运行在所声称的平台、基线是否足够强、统计是否完整；
     - 专门审查图表、定性案例、失败案例、可复现性和论文—代码—数据一致性；
     - 第一轮同样只读、独立给出拒稿意见。

  主代理不得把三个角色的意见简单平均。要像 Area Chair 一样辨别哪些是致命问题、哪些是可修问题，并建立 `小论文-2/REVIEW_LOG.md`，记录每轮：

  - 两名审稿人的独立意见；
  - 裁判裁决；
  - 运动员的修改和证据；
  - 审稿人复审结论；
  - 尚未解决的问题。

  每次算法发生实质变化后，必须让两位审稿人重新审阅修改后的实现、实验与论文。至少进行“初审—修改—复审”两轮；存在重大问题就继续迭代，不能因为文字变好看就宣布完成。

  三、先做真正的新颖性审计

  当前二区稿可能存在以下风险，请逐项验证，不要预设它们已经解决：

  - 威胁度与差异化安全裕度可能只是常见风险缓冲；
  - 预测误差占据管可能只是标准几何膨胀；
  - 到达时间投影和先通过/后通过窗口已有大量相关工作；
  - 时间同伦图可能只是标准分层图或 beam search 的重新命名；
  - 滚动同伦锁定可能只是工程滞回机制；
  - 连续分割定理可能仅是排序后枚举一维间隙的自然性质；
  - Top-K 空间候选可能直接落入已有 K-best 动态规划；
  - Apollo 双 QP 集成属于工程兼容性，不自动构成算法创新；
  - 大量自生成场景不能替代公开基准、真实系统或强外部方法。

  建立：

  - `小论文-2/NOVELTY_AUDIT.md`
  - `小论文-2/RELATED_WORK_MATRIX.md`

  其中必须把每条论文主张映射到最近先行工作，明确“相同点—不同点—新增技术困难—可验证优势”。只引用真实存在并实际核验过的原始论文，不得根据标题猜内容。

  四、重新确定唯一、足够硬的核心贡献

  当前稿件不能继续靠“很多模块叠加”制造创新感。裁判必须从审计结果中确定一个清晰的中心问题，例如：

  “在保留路径—速度解耦凸 QP 的前提下，如何有限、可验证地覆盖动态多障碍物场景中的关键空间—时间联合同伦，并在滚动规划中保持安全和决策一致性？”

  这只是候选方向，不得机械照抄。应比较至少三种可行的核心升级路线，然后选择最有学术价值且能够真实实现、证明和验证的一种。

  优先考虑能形成以下一种或多种实质结果的路线：

  - 空间与时间标签的统一联合同伦图，而不是两个普通候选模块的串联；
  - 对明确场景模型的同伦覆盖完整性；
  - 有界复杂度、次优性界或候选剪枝不丢失可行解的条件；
  - 有界预测误差下的安全保证；
  - 滚动重规划下的递归可行性或承诺一致性；
  - 失败时具有严格定义的安全降级策略；
  - 相比联合时空搜索，明确说明保留 QP 结构所付出的可量化近似代价。

  理论不能只停留在重新包装显然结论。每个定理都必须具备：

  - 精确定义和适用条件；
  - 完整证明；
  - 与实际代码一一对应；
  - 随机性质测试、穷举 oracle 或自动反例搜索；
  - 对不满足条件的反例和边界说明。

  如果无法形成足够强的核心算法，就诚实判断二区目标尚未达到，并继续研究，不得用“统一框架”四个字代替创新。

  五、重构论文叙事：算法立足，Apollo 落地

  用户明确反对以 Apollo 为论文中心。全文必须遵循：

  1. 标题、摘要、引言、问题定义和方法主体首先描述一般性的路径—速度解耦规划问题与算法。
  2. 不要再使用“2.1 Apollo 的解耦求解主干与约束接口”这类标题。
  3. 第二章应使用类似：
     - “路径—速度解耦规划与问题形式化”
     - “解耦规划中的时空同伦缺失”
     - “问题定义”
  4. 数学方法必须独立于 Apollo 类名成立。
  5. Apollo 只在“系统实现与平台集成”小节中出现，用来说明算法如何接入已有求解链。
  6. 必须准确区分 Apollo 已有内容和本文原创内容。
  7. 如果实验实际上没有在真实 Apollo/CyberRT/Planning 模块中运行，就不得仅凭“流程等价”宣称为真实 Apollo 实验；要按可核验证据准确描述平台。
  8. 不得将 Python 复现、接口映射或算法级模拟冒充真实车端 Apollo 部署。

  六、建立以图为入口的视觉论证

  当前论文图表过少、数据表过多，读者无法直观看懂算法和结果。不要只是增加装饰图，而要构建一条“先看图就能理解论文”的视觉主线。

  至少审查并考虑制作以下图形：

  1. 首页概念图/teaser：
     - 同一多障碍物场景下，传统解耦规划为什么丢失可行解；
     - 本文如何保留空间与时间联合同伦；
     - 用轨迹、障碍物和时序直接展示差异。

  2. 平台无关的总体算法框架图：
     - 输入、鲁棒占据、联合同伦生成、候选筛选、QP 约束编译、滚动反馈；
     - 主图中不要把 Apollo 放在中心。

  3. 空间同伦原理图：
     - 展示 $2^k$ 组合如何变成有限连续分割；
     - 展示 Top-K 跨纵向分组保持连续性的过程；
     - 明确什么是本文新算法，什么是普通排序或动态规划。

  4. 时间同伦与 ST 走廊图：
     - 同时显示 pass-before、yield-after 和混合时间窗；
     - 显示这些窗口如何转为双向 ST 上下界。

  5. 典型场景轨迹对比图：
     - 至少覆盖窄路多障碍物、动态横穿、预测含噪和滚动承诺；
     - 同图比较强基线、消融和完整方法；
     - 不能只画最终轨迹，还应显示安全走廊、预测占据和最小间距。

  6. 统计实验图：
     - 各场景族成功率和碰撞率及置信区间；
     - 运行时间分布，而不只是 P50/P95 表格；
     - 大规模消融效应及区间；
     - 不应只使用柱状图堆数字，应让图回答具体审稿问题。

  7. 失败案例和适用边界图：
     - 展示算法在哪些条件下失效；
     - 说明安全降级是否生效。

  优先使用论文原生的 TikZ、PGFPlots 或由真实 CSV/JSON 自动生成的矢量图。所有实验图必须由数据脚本生成，禁止手工调整图中结果。检查字体、单位、图例、色盲可读性、双栏缩放和黑白打印效果。

  建立 `小论文-2/FIGURE_PLAN.md`，为每张图写清：

  - 它回答哪个审稿问题；
  - 使用什么真实数据；
  - 读者应该一眼得到什么结论；
  - 放在论文哪个位置。

  七、实验必须真正支撑二区主张

  算法变化后，所有受影响结果必须重新生成，不得沿用旧数字。

  实验至少检查：

  - 公平的时间盲基线；
  - 强解耦/迭代基线；
  - 可核验的联合时空参考；
  - 至少一个真正相关的外部竞争方法；若没有忠实实现，不得冒充复现；
  - 公开场景、公开数据集或标准化仿真场景；
  - 开环随机场景；
  - 多周期滚动规划；
  - 预测误差和参数扰动；
  - 组件消融；
  - 失败模式；
  - 完整规划周期耗时及尾延迟；
  - 碰撞、最小间距、TTC、到达率、进度、舒适性和计算开销。

  同一方法的参数、硬件、时间预算和碰撞判定必须公平。所有主要结论提供配对统计、置信区间和效应量。不能只因为样本量大、p 值很小就声称算法创新显著。

  八、完成门槛

  只有同时满足以下条件，才能由主代理判定目标完成：

  - 二区稿有一个可以一句话说清、不是 Apollo 重命名或模块堆叠的核心创新；
  - 最近相关工作无法直接覆盖该核心贡献；
  - 至少一个非平凡理论性质由证明和自动验证共同支持；
  - 代码真实实现论文所述算法；
  - 算法变化后的实验全部重新生成；
  - 至少一个外部或标准化验证来源，不完全依赖自生成场景；
  - 图形能够独立讲清问题、方法、结果和失败边界；
  - 中英文稿的方法、公式、数据和结论一致；
  - `CLAIM_TRACEABILITY.md` 更新完整；
  - 完整测试、静态检查、实验复现、LaTeX 编译和 PDF 视觉检查通过；
  - `git diff --check` 通过，工作区状态清楚；
  - 两位审稿人在最终独立复审中，对“创新性、技术正确性、证据充分性、表达与图表”均无致命问题；
  - 主代理给出类似 Area Chair 的最终判断：Reject / Major Revision / Weak Accept / Accept，并列出依据。

  评分不得自我放水。建议使用 1–5 分制，创新性、正确性、实验、表达均达到 4 分，且研究诚信项通过，才可以判断具备正式送审条件。

  若暂时无法达到这些门槛，保持 Goal 进行中，继续解决最关键的问题；不要因为完成了一版 PDF 或测试通过就提前结束。

  最终交付至少包括：

  - 修订后的中文稿和英文 IEEE 稿；
  - 可编译 PDF；
  - 新颖性审计和相关工作矩阵；
  - 每轮交叉审阅记录；
  - 图形设计说明和全部图源；
  - 算法代码、测试及自动反例验证；
  - 完整实验数据和生成脚本；
  - 更新后的复现说明与主张追踪表；
  - 两稿重合及投稿伦理说明；
  - 清晰的 Git 提交；
  - 一份直白的最终裁决，说明稿件究竟是否已经达到二区竞争水平，以及还存在哪些无法掩盖的风险。

  现在先读取历史会话、当前代码和两份论文，创建 Goal，启动三个子代理完成第一轮独立审阅，然后由主代理作出第一轮裁决并立即推进实质修改。不要只输出计划后等待我确认。