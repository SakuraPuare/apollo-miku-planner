% 路径与速度规划之间的单向信息传递与时空约束缺口
% 漏斗左侧列出 PathBoundsDecider 在做时变 SL 投影时已经掌握的全部信息
% 右侧列出 SpeedBoundsDecider 实际能拿到的信息
% 漏斗体现 Apollo 解耦框架的窄通道：仅 [l^-(s), l^+(s)] 通过
% 本章新增的传递载体（path 截断 + 时间清单 T）放在漏斗下方作为补丁
\begin{tikzpicture}[scale=0.95, transform shape,
    infobox/.style={rectangle, rounded corners=4pt, draw=deepblue, fill=lightblue!30,
        minimum width=3.8cm, align=left, font=\small, inner sep=8pt},
    recvbox/.style={rectangle, rounded corners=4pt, draw=deepblue, fill=bglight,
        minimum width=3.8cm, align=left, font=\small, inner sep=8pt},
    lostitem/.style={rectangle, rounded corners=2pt, draw=dangerred!70, fill=dangerred!8,
        font=\small, inner sep=4pt},
    addbox/.style={rectangle, rounded corners=3pt, draw=lightgreen!60!black, fill=lightgreen!18,
        font=\footnotesize, align=left, inner sep=5pt},
]

% ===== Left: PathBoundsDecider 在 SL 阶段已掌握的信息 =====
\node[infobox, minimum height=4.4cm] (pathbox) at (0, 0) {%
    \textbf{Path 阶段已掌握}\\[5pt]
    \textcolor{deepblue}{$\bullet$}~横向边界 $[l^-(s),\,l^+(s)]$\\[3pt]
    \textcolor{deepblue}{$\bullet$}~分割点 $p^\star$ 与最优间隙 $g_{p^\star}(s)$\\[3pt]
    \textcolor{deepblue}{$\bullet$}~到达时间 $\tau(s)$\\[3pt]
    \textcolor{deepblue}{$\bullet$}~动态障碍 $\tau$ 时刻 SL 投影\\[3pt]
    \textcolor{deepblue}{$\bullet$}~关键时间点 $\{(s_k,\,\tau_k)\}$%
};

% ===== Right: SpeedBoundsDecider 实际接收的信息 =====
\node[recvbox, minimum height=3.4cm] (speedbox) at (11.5, 0) {%
    \textbf{Speed 阶段实际接收}\\[5pt]
    \textcolor{deepblue}{$\bullet$}\;$[l^-(s),\,l^+(s)]$\\[1pt]
    {\footnotesize\textcolor{deepblue}{知道路径在哪里}}\\[6pt]
    \textcolor{dangerred}{$\times$}\;其余时空决策依据\\[1pt]
    {\footnotesize\textcolor{dangerred}{不知道路径为何成立}}%
};

% ===== Middle: 传递瓶颈 =====
\coordinate (fTL) at (4.6, 1.9);
\coordinate (fBL) at (4.6,-1.9);
\coordinate (fTR) at (7.8, 0.55);
\coordinate (fBR) at (7.8,-0.55);

\fill[bglight, draw=deepblue!60, thick]
    (fTL) -- (fTR) -- (fBR) -- (fBL) -- cycle;

\node[font=\small\bfseries, deepblue!80] at (6.1, 2.3) {Apollo 解耦瓶颈};

% 单根蓝色箭头从 pathbox 穿过漏斗到 speedbox
\draw[deepblue, very thick, -Stealth] (pathbox.east) -- (speedbox.west)
    node[midway, above, font=\small\bfseries, deepblue] {$[l^-(s),\,l^+(s)]$};

% ===== 丢失的信息：被漏斗壁挡住的载体 =====
\node[lostitem] (lpstar) at (5.3, 3.3) {$p^\star,\,g_{p^\star}(s)$};
\node[lostitem] (ltau) at (8.3, 3.3) {$\tau(s)$};
\draw[dangerred, thick, -|] (lpstar.south) -- ($(fTL)!0.22!(fTR)$)
    node[pos=0.45, right, font=\small\bfseries, dangerred] {$\times$};
\draw[dangerred, thick, -|] (ltau.south) -- ($(fTL)!0.78!(fTR)$)
    node[pos=0.45, right, font=\small\bfseries, dangerred] {$\times$};

\node[lostitem] (lT) at (6.3, -3.3) {$\{(s_k,\,\tau_k)\}$ 时间清单};
\draw[dangerred, thick, -|] (lT.north) -- ($(fBL)!0.5!(fBR)$)
    node[pos=0.45, right, font=\small\bfseries, dangerred] {$\times$};

% ===== 本章新增的两条补丁通道（标在漏斗下方）=====
\node[addbox] (chA) at (1.6, -3.3) {
    \textbf{隐式通道}\\
    $g_{p^\star}{<}W_{ego}$ $\Rightarrow$ path 在 $s_k$ 截断\\
    Speed 视野自然受限
};
\node[addbox] (chB) at (10.6, -3.3) {
    \textbf{显式通道}\\
    时间清单 $\{(s_k,\,\tau_k)\}$ 透传\\
    收紧 $s_j^{ub}{\leftarrow}\min(s_j^{ub},\,s_k)$
};
\draw[lightgreen!60!black, very thick, dashed, ->] (chA.east) -- (chB.west)
    node[midway, above, font=\scriptsize, lightgreen!60!black] {本章建立的双通道，绕过解耦瓶颈};

\end{tikzpicture}
