% ============================================================
% fig_sys_arch.tex  改进后系统架构图（Beamer版，三层设计）
% 依赖：fig_colors.tex 中定义的颜色
% 用法：\input{figures/fig_sys_arch.tex}
% 设计尺寸：约 10cm × 5.5cm，适合单页 Beamer 帧
% ============================================================
\begin{tikzpicture}[
  imod/.style={rectangle, draw=deepblue!80, fill=inputyellow,
               minimum width=2.15cm, minimum height=0.62cm, font=\scriptsize},
  planbox/.style={rectangle, draw=deepblue, fill=bglight,
                  minimum width=10.2cm, minimum height=3.75cm},
  fullrow/.style={rectangle, draw=deepblue, fill=skyblue,
                  minimum width=9.5cm, minimum height=0.52cm, font=\scriptsize},
  submod/.style={rectangle, draw=deepblue, fill=lightblue,
                 minimum width=2.75cm, minimum height=0.58cm, font=\scriptsize},
  impmod/.style={rectangle, draw=deeppink, fill=improvedpink,
                 minimum width=2.75cm, minimum height=0.58cm, font=\scriptsize},
  outmod/.style={rectangle, draw=deepblue, fill=outputpink,
                 minimum width=9.5cm, minimum height=0.62cm, font=\scriptsize\bfseries},
  arr/.style={-Stealth, thick, deepblue}
]

%% 输入层
\node[imod] (routing)    at (-3.8, 0)  {\parbox{2.0cm}{\centering Routing\\参考线}};
\node[imod] (perception) at (-1.3, 0)  {\parbox{2.0cm}{\centering 感知\\障碍物}};
\node[imod] (prediction) at ( 1.3, 0)  {\parbox{2.0cm}{\centering 预测\\轨迹}};
\node[imod] (vehicle)    at ( 3.8, 0)  {\parbox{2.0cm}{\centering 车辆\\状态}};

%% 输入层标签
\node[font=\footnotesize\bfseries, color=deepblue] at (0, 0.65) {输入层};

%% 规划处理大框
\node[planbox] (planbox) at (0, -2.65) {};

%% 处理层标题（框内顶部）
\node[font=\footnotesize\bfseries, color=deepblue] at (0, -1.05)
  {处理层（Planning 模块）};

%% 参考线处理（全行）
\node[fullrow] (refrow) at (0, -1.6) {%
  \parbox{9.2cm}{\centering 参考线处理：接收 $\rightarrow$ 平滑 $\rightarrow$ Frenet 坐标系转换}};

%% 障碍物处理标题
\node[font=\scriptsize\bfseries] at (0, -2.22) {障碍物处理（改进模块）};

%% 障碍物三子模块
\node[submod] (obsfuse) at (-3.3, -2.8)
  {\parbox{2.55cm}{\centering 障碍物融合\\与分类}};
\node[impmod] (threat)  at ( 0.0, -2.8)
  {\parbox{2.55cm}{\centering 威胁度评估\\（改进）}};
\node[impmod] (stbound) at ( 3.3, -2.8)
  {\parbox{2.55cm}{\centering 动态权重\\ST 边界构建}};

%% 路径-速度规划（全行）
\node[fullrow] (pathrow) at (0, -3.7) {%
  \parbox{9.2cm}{\centering 路径-速度规划：路径 QP $\rightarrow$ 速度 QP $\rightarrow$ 轨迹耦合}};

%% 输出层
\node[font=\footnotesize\bfseries, color=deepblue] at (0, -4.42) {输出层};
\node[outmod] (output) at (0, -4.9) {%
  \parbox{9.2cm}{\centering ADCTrajectory（位置、速度、加速度、时间戳）}};

%% 输入箭头（向下进入规划框）
\draw[arr] (routing.south)    -- ++(0,-0.25);
\draw[arr] (perception.south) -- ++(0,-0.25);
\draw[arr] (prediction.south) -- ++(0,-0.25);
\draw[arr] (vehicle.south)    -- ++(0,-0.25);

%% 障碍物子模块间箭头
\draw[arr] (obsfuse) -- (threat);
\draw[arr] (threat)  -- (stbound);

%% 规划到输出箭头
\draw[arr] (pathrow.south) -- (output.north);

\end{tikzpicture}
