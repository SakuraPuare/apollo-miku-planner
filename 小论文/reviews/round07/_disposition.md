# Round 07 处置单（责任编辑）

输入稿 `drafts/v7.tex` → 输出稿 `drafts/v8.tex`。本轮四份评审（theory/apollo/method/writing）
**全部 P0=0、major=0**，仅 minor。编辑原则：把所有"改文字即清"的 minor 逐条小段 Edit 落实，
需新实验/需人工拍板的记入下方"不动稿登记"。

变更度量（line-diff 失真，LaTeX 段落为单行长行，故用字节与词级 diff 计量）：

- 字节增量：v8 比 v7 多 `1932` 字节。
- 词级 diff（`git diff --word-diff=porcelain`）配对改动块：`20` 处。
- 编译：`latexmk -xelatex v8.tex` exit=0，biber 已跑，末轮 log 中 `Citation ... undefined` 计数 `0`，
  无 rerun 警告，产出 30 页 `v8.pdf`。

---

## 一、已改（改文字即清的 minor，逐条附 grep 证据）

### 理论审（theory.md，3×minor 全清）

**m7-1｜命题 feasibility_recovery 证明中"构造性论证"与"存在性前提"分工未点死**
- 处置：证明段补一句把二者层次讲死——等待型剖面构造仅为前提在"不后退型下界"常见情形下被满足的充分构造，结论本身仅依赖可行解存在前提，故对下界正负不敏感。
- 证据 `grep -c "需明确二者的分工" v8.tex` → 1：
  > 需明确二者的分工，沿减速或保持方向构造等待型剖面给出的是前提在不后退型下界这一常见情形下被满足的一个充分构造，而结论本身仅依赖可行解存在前提，故对下界正负不敏感。

**m7-2｜§3.4 式 time_varying_opt 处"退化"用词与定理逐截面陈述有张力**
- 处置：把"逐截面最优性在此约束下退化为对最薄截面的保证"改述为正面的"逐截面等式沿固定分割复合"。
- 证据 `grep -c "逐截面等式沿固定分割复合后" v8.tex` → 1：
  > 定理\ref{thm:max_gap}的逐截面等式沿固定分割复合后，整段宽度由最薄截面 $\min_s g_p(s)$ 决定，受限最优即在 $k+1$ 个候选分割上取其最大，约束施加于可行解集合而非削弱定理结论本身。

**m7-3｜§3.6 逐段复杂度清单缺交互密度邻域查询项，与 §4.3 口径落差**
- 处置：在 §3.6 逐段相加处补桶索引摊还 $O(n)$ 一句并交叉引用 §4.3。
- 证据 `grep -cF '交互密度因子的邻域统计经空间桶索引摊还为' v8.tex` → 1：
  > 交互密度因子的邻域统计经空间桶索引摊还为 $O(n)$，在密度有界前提下不抬阶，详见 §\ref{sec:thy_complexity}。

### Apollo 工程审（apollo.md，3×minor 全清）

**A-1｜SpeedBoundsDecider 在 lane_follow 运行两次（PRIORI/FINAL），双源取最小处泛指未点明**
- 处置：在双源融合处补明此处指 pipeline 末次 FINAL 调用所产 ST 边界。
- 证据 `grep -cF 'FINAL 调用所产 ST 边界，亦即与速度二次规划直接衔接者' v8.tex` → 1：
  > 此处 \texttt{SpeedBoundsDecider} 指 pipeline 末次即 \texttt{SpeedDecider} 之后的 FINAL 调用所产 ST 边界，亦即与速度二次规划直接衔接者。

**A-2｜use_st_drivable_boundary 开启时 PathTimeHeuristicOptimizer 会意外消费 MIKU 写入边界，"闲置"前提需限定配置**
- 处置：在复用声称处补限定，明确无副作用性仅成立于该开关默认关闭的生产配置。
- 证据 `grep -c "上述复用以" v8.tex` → 1：
  > 上述复用以 \texttt{use\_st\_drivable\_boundary} 默认关闭为前提，本文接入运行于该开关关闭的生产配置下；该开关若被开启，则 \texttt{PathTimeHeuristicOptimizer} 亦会读取经本文重写的该字段，故复用的无副作用性限定在默认配置之内。

**A-3｜STDrivableBoundary 原生消费者是 PathTimeHeuristicOptimizer 的 DP 代价，未点名**
- 处置：在"下游消费受开关门控"处点名原生消费者，坐实 MIKU 系重定向而非简单填充。
- 证据 `grep -c "其唯一原生消费者为" v8.tex` → 1：
  > 其唯一原生消费者为 \texttt{PathTimeHeuristicOptimizer} 的动态规划代价项，且该消费受……门控、默认关闭。

### 方法审（method.md，3×minor 全清）

**minor-1｜纵向平顺改善实证基础实为 n=2（C1、C2 派生自失效构型），舒适性结论处未显式设限**
- 处置：在舒适性结论段末把"仅 C1/C2 承载"与"派生自失效构型"两点交汇为一句设限。
- 证据 `grep -cF '样本数 $n=2$ 且非独立采样' v8.tex` → 1：
  > 换言之，纵向平顺改善这一正面结论的实证基础为 C1、C2 两个派生自失效构型的场景，样本数 $n=2$ 且非独立采样，其向独立常规工况的推广有待补充。

**minor-2｜±20% 扰动幅度与 100 次采样数缺选取依据**
- 处置：补 ±20% 作为人工标定合理容差的动机，及 100 次为固定采样预算、以极值统计量趋稳为度。
- 证据 `grep -c "合理容差范围，用以考察决策对标定误差的鲁棒性" v8.tex` → 1，`grep -c "100 次为确定性复现下的固定采样预算" v8.tex` → 1：
  > 此处$\pm 20\%$取作威胁度权重人工标定在工程实践中的合理容差范围，用以考察决策对标定误差的鲁棒性，100 次为确定性复现下的固定采样预算、以极值统计量趋于稳定为度。

**minor-3｜通过判据 1 m 容差未给依据，亦未声明 0/4 与 4/4 对该阈值不敏感**
- 处置：补容差语义 + 不敏感性论证（Baseline 停车降级终点里程远低于阈值、MIKU 完整到达，相差一整段里程）。
- 证据 `grep -cF '容差用于吸收离散终点的数值余量' v8.tex` → 1：
  > 该$1$\,m容差用于吸收离散终点的数值余量；因压力场景中Baseline触发停车降级、其终点里程远低于阈值，而MIKU完整到达终点，二者在$s_{\mathrm{end}}$上相差一整段可通行里程，故0/4与4/4的通行结论对该容差在合理区间内的取值不敏感。

### 写作/期刊审（writing.md，5×minor 中 3 清 + 1 deferred + 1 闸门顺延）

**m7-1｜第 413 行 STDrivableBoundary 段为全文最长句（≈185 汉字九重动作），第 422 行近距同义重复**
- 处置：把该长句按"原生闲置事实 / 本文重建写入 / 跨阶段持久 / QP 收紧"以句号断为多句；删去第 422 行重复的"精确等待截止时间"措辞之一。
- 证据 `grep -cF '亦不读取它。本文重新利用这一既有闲置数据结构' v8.tex` → 1（原"它；本文"已断句为"它。本文"）；
  第 422 行重复词已删，`grep -cF '走廊约束给出精确的等待截止时间 $\tau(s_k)$，把基于全时刻并集投影' v8.tex` → 0（原句删除）。改后为：
  > 含动态障碍物时，走廊约束把基于全时刻并集投影的保守让行替换为按 $\tau(s_k)$ 的精确等待截止时间……

**m7-3｜PathTimeHeuristicOptimizer / SpeedDecider / STBoundsDecider 首现未给中文功能定位，与 PathBoundsDecider 体例不一**
- 处置：三处首现各补四到六字中文功能定位。
- 证据 三处 `grep -c` 均 → 1：
  > 这一 ST 图动态规划粗搜索环节在 / 这一纵向超车让行决策环节标定 / 这一 ST 边界决策环节映射为

**m7-4｜"实验分为 4 部分"与 §5 实际六小节对不上**
- 处置：改为"四类结果 + 另含平台设置与局限各一节"使计数与小节结构对齐。
- 证据 `grep -c "实验给出四类结果，依次是" v8.tex` → 1：
  > 实验给出四类结果，依次是……另含平台与场景设置、局限性讨论各一节。

---

## 二、压下/顺延（本轮不在 v8 落实，理由如下）

**m7-2（writing）｜references.bib 87 条目、实引 17 键，约 70 条零引用未清理**
- 性质：`sorting=none` 下未引用条目不进文献表、编译无害（本轮 v8 编译 0 悬空引用已证）。
  评审本人定性为"定稿闸门必办项"，非当前轮内容硬伤。
- 顺延理由：删 bib 属投稿前定稿动作（GOAL 阶段 E），且需逐键核对保留集合，
  与中文审阅阶段解耦；连续五轮均按此判断顺延。`grep -c '^@' references.bib` → 87（维持）。
- 登记：MODIFICATIONS「⑥文风与格式」定稿闸门，待阶段 E 一次性收敛到 17 键。

**m7-5（writing）｜引言末显式贡献凝练句**
- 性质：评审明确"留记录不强制、属英文转换阶段可选润色"，且强调不得退化为三贡献清单。
- 顺延理由：当前引言连贯陈述已承载单 insight（合规），加凝练句是英文阶段可选项。
  `grep -cF '综上，本文的贡献在于在解耦接口处建立显式约束传递通道' v8.tex` → 0（按评审意见不强制，维持现状）。
- 登记：MODIFICATIONS「④叙事与贡献」英文阶段可选。

---

## 三、不动稿登记（数据红线挡住，需新实验/需人工拍板）

四份评审一致确认以下各项文字防御已在位、本轮不重复扣分，受 SPEC B/E 数据冻结红线挡住，**不动稿**，
统一记入 MODIFICATIONS「⑧需新实验或人工决策」：

1. 走廊注入组件激活场景验证（M4≈M5 根因，标题级 insight 注入侧零实验证据）—— apollo N-1 / method #1 / writing。最高优先级。
2. 最小安全间距 / 几何侵入校验（差异化裕度压至 0.10 m + 4/4 通过 + 判据无碰撞校验）—— method #2 / apollo。
   method 提示：若冻结资料含逐场景 SL 几何，可由 $l(s)$ 与障碍物投影后处理反算，从"新仿真"降为"分析补齐"，**需人工先核样本可得性**。
3. Baseline 启用 LaneBorrowPath 后在 P4 的对照 —— method #3。低成本新实验。
4. QP 墙钟耗时 N 次重复 + 方差/中位数 —— method #4 / apollo N-2（C++ 运行时未计时）。
5. 灵敏度分布矩补全 + 场景四 57% 翻转样本重跑确证 —— method #5。
6. 感知/预测噪声灵敏度 —— method #6。

跨视角提醒（theory→apollo）：稿中保留的"SpeedBoundsDecider 纵向下界为不后退型、超车让行体现为 ST 上界收紧而非正下界"
断言已被 m7-1 解耦为充分性说明，定理成立不再依赖其真伪；apollo.md §1 已对 STDrivableBoundary 生产者/消费者/门控逐句源码坐实，无需改稿。

---

## 四、一句话小结

本轮 4 persona 全部 P0=0 major=0；共落实 **12 条 minor**（theory 3 + apollo 3 + method 3 + writing 3），
压下/顺延 **2 条**（bib 清理与贡献凝练句，均属定稿/英文阶段），不动稿登记 **6 类需新实验**。
v8 较 v7 增 1932 字节 / 20 处词级改动，`latexmk -xelatex` 编译通过、0 悬空引用、30 页 PDF。
