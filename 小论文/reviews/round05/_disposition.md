# Round 05 处置单（责任编辑第 5 轮）

输入稿 `drafts/v5.tex` → 输出稿 `drafts/v6.tex`。编译 latexmk -xelatex 通过（exit=0，686203 bytes，29 页）。
git diff --no-index v5/v6：1 file changed，19 insertions / 19 deletions（全部为定点行内替换，对应下列 16 条改稿点）。

四份评审本轮统计：theory P0=0 major=2 minor=4；apollo P0=0 major=0 minor=4；method P0=0 major=0 minor=4；writing P0=0 major=2 minor=4。无 P0。

---

## 已改（逐条附 grep 证据）

### theory M5-2（major）命题 prop:decoupled_infeasible 单障碍物并集与「多障碍物」定位的覆盖缺口
取审稿建议其二（省力且不动公式）：显式划分覆盖责任，时间维单障碍封锁归本命题、空间维联合封锁归定理 thm:max_gap。
证据（v6:491）：
> 需补充其覆盖范围：式 \eqref{eq:thy_infeasible_cond} 刻画单个动态障碍物时间扫掠并集封锁整段可行区间这一类时间维封锁；多个障碍物各自只封一部分、其空间并集才覆盖 $\mathcal{B}_{W}(s)$ 的联合封锁，在时不变并集投影下由定理 \ref{thm:max_gap} 的 $\max_p g_p < W_{\text{ego}}$ 判据处理，即时间维单障碍封锁归本命题、空间维联合封锁归定理 \ref{thm:max_gap}，二者拼出对解耦失效的完整覆盖。

### theory M5-1（major）命题 prop:feasibility_recovery 等待型剖面可达性依赖未写明的下界语义
取审稿建议其一（最干净、贴合 Apollo）：点明所注入下界为 SpeedBoundsDecider 的「不后退型」纵向下界，非强制前进的正下界，故减速/保持剖面天然满足。apollo 审已交叉确认 11.0 下界语义。
证据（v6:575）：
> 这里所注入的速度二次规划下界为 \texttt{SpeedBoundsDecider} 给出的纵向下界，其语义为不后退型而非强制自车在某时刻前到达某绝对位置的正下界，故任意减速或保持的剖面天然满足该下界。

### writing M5-1 / apollo A-1（major / minor，连续多轮遗留）摘要匿名「某生产级自动驾驶栈」与正文直名 Apollo 矛盾
证据（v6:94）：
> MIKU 以增量方式接入 Apollo 11.0 生产级自动驾驶栈的路径边界构建环节

### writing M5-2（major，连续五轮遗留）结论第 870 行第二摘要式数字堆砌
删逐场景 QP、消融 100/99.18/37.13、灵敏度 6.4%/57% 等细节，仅留 0/4→4/4 与 29.3% 两个结论性判断 + 局限指向。
证据（v6:870）：
> 消融、灵敏度与逐场景求解耗时的完整结果见第 \ref{sec:experiment} 节，其中走廊注入组件因当前压力场景未激活其活跃边界而在本组消融中贡献不显，最小安全间距、统计检验与走廊激活场景验证等局限于下节集中说明。

### apollo A-2（minor）第 685 行 FallbackPath 降级下「QP 提前中止/不完整计算」误述
按源码事实改为「降级停车问题、非同一规模」（速度链路在回退路径上跑完整流程）。
证据（v6:685）：
> Baseline路径阶段判定阻塞后回退至FallbackPath停车，其速度求解针对的是一个短路径或停车的降级问题，与MIKU在完整通行路径上的求解非同一规模，记录耗时不构成两种算法的对等效率比较依据。

### apollo A-3（minor）第 376/568 行盒约束产出链略去 SpeedBoundsDecider
两处均补全因果链（DP 粗搜索 + SpeedDecider 标定决策 + SpeedBoundsDecider 产出/映射）。
证据（v6:376）：
> \texttt{SpeedDecider} 标定 YIELD/OVERTAKE 纵向决策、再由 \texttt{SpeedBoundsDecider} 映射为 $ST$ 边界所得的线性盒约束

证据（v6:568）：
> \texttt{SpeedDecider} 标定让行或超车、再由 \texttt{SpeedBoundsDecider} 产出的线性约束在二次规划结构上一致

### apollo A-4（minor）第 348 行预测时域错配（6s/5s/7s）未点明 τ(s) 查询时域
证据（v6:348）：
> 即路径阶段时变投影以 Prediction 输出的 $6$\,s 预测轨迹为源，超出该视野的部分按上述兜底延拓，其与速度阶段 $7$\,s ST 视野的衔接由该越界保守延拓处理。

### method minor-1（minor）「七项指标」与枚举六项对不上
证据（v6:617）：
> 每个场景计算六项指标，覆盖通行能力、轨迹舒适性与求解开销三类。

### method minor-2（minor，round04 遗留）综合评分「折半惩罚」无依据 + 评分自定义
取建议 (b)：主结论回归无自由参数的通过数，综合评分降为辅助排序；并把「折半惩罚」改述为「保守经验折减系数 0.5」并声明主结论对其不敏感。
证据（v6:754）：
> 退回算术平均并乘以一个保守经验折减系数 $0.5$ … 本节消融的主结论建立在无自由参数的客观量即各变体的通过数之上 … 故主结论对该系数取值不敏感。

### method minor-3（minor）第 559 行「满足实时性要求」与「不外推」自我设限张力
证据（v6:559）：
> 在算法复杂度上具备满足 Apollo 11.0 的 $100$~ms 规划周期的可行性，桌面复现耗时远低于该周期，车载嵌入式平台的实时性须另行评估，本工作不就此外推。

### method minor-4（minor）可比场景控制口径精确化
证据（v6:641）：
> 其控制的是障碍物构型族，未控制速度区间与限速，故可比场景的结论限于同等可达前提下的构型级对照，速度类与舒适性类指标不在压力组与可比组之间直接比较。

### writing m5-2（minor）「五因子」计量体例残字（M4-1 收尾最后一处）
证据（v6:784 → grep "五因子" 全文计数 = 0）：
> 5 个因子的权重独立同分布地施加均匀扰动后重新归一化使权重和回到 1

### theory m5-4（minor）枚举--博弈 O(mn) 与 MIKU 的 n 符号串台
全文三处 $O(mn)$（intro 第 107 行、相关工作第 823 行、表 tab:related 第 849 行）统一改为 $O(m_p m_v)$ 并注明与障碍物数 n 不同义。grep "O(mn)" 计数 = 0。
证据（v6:823）：
> 组合规模为 $O(m_p m_v)$，其中 $m_p$、$m_v$ 分别为候选路径数与候选速度数，与本文 §\ref{sec:method} 中表征障碍物数的 $n$ 不同义

### theory m5-1（minor）命题 prop:decoupled_infeasible 结论隐含「路径必经 [s_a,s_b]」前提
证据（v6:486）：
> 在自车须沿参考线穿越 $[s_a, s_b]$ 的前提下即 $\mathcal{F}_{\text{dec}} = \emptyset$

### theory m5-2（minor）式 eq:time_varying_opt 的 W* 最优性未给一句论证
证据（v6:369）：
> 式\eqref{eq:time_varying_opt}本身可由定理\ref{thm:max_gap}直接得到，由各截面 $W(p,s)=g_p(s)$，固定分割 $p$ 的整段通行带宽度为 $\min_s g_p(s)$，在 $k+1$ 个候选分割中取最大即得 $W^{*}$

### theory m5-3（minor）引理 lem:boundary_trim 规则 B 抬高下界基线后 g_0 端部间隙说明
证据（v6:523）：
> 越界障碍物的 $v_i$ 抬高全局下界基线 $l_{road}^{-}\to\max(l_{road}^{-},v_i)$ 后，剩余障碍物因未越界仍满足 $u_j\geq l_{road}^{-}$，端部间隙 $g_0$ 相应以抬高后基线计算，定理前提不破坏。

---

## 压下/不在本轮动稿

### writing m5-1（minor，连续五轮遗留）超长句拆分
压下。属全文多处润色项（引言 111、§3 224、方法 413、理论 489/577、§5.3 723、§5.5 786、结论 868/870），本轮新增/改动文本已尽量控制句长；系统性断句宜在英文转换前统一处理，避免本轮逐句改动放大 diff、引入新长句。登记续待。

### writing m5-3（minor，连续多轮遗留）bib 未引用条目约 70 个清理
压下，不动。改 references.bib 而非正文；sorting=none 下未引用条目不进文献表、编译无害。删条目有误删正文实引键、破坏编译的风险，且属「定稿前」清理项（writing 审亦标"定稿前必须清理"）。留待投稿前定稿阶段统一核对 cite↔bib 后批量清理。

### writing m5-4（minor）摘要首数单位
已随 M5-1 同处一并修（v6:94「由 3.53 m/s 升至 4.57 m/s」），见上 grep 证据区。实为已改。

### writing m5-5（minor）摘要末句 C2 口径——审稿标注「已解决，仅留记录」，无需动。

---

## 需新实验 / 需人工拍板（数据红线挡住，仅登记，不动稿）

均沿 method 审与 writing 审，文字防御已在位，本轮不动数据：

1. （最高优先级，自 round03 遗留）走廊注入组件激活场景验证——坐实标题级 insight 注入侧、修 M4≈M5 数据根因，须补能触发活跃时间窗约束的动态冲突密集场景。需新实验。
2. （性价比最高，需人工先核可否从冻结 SL 几何反算）最小安全间距 / 几何侵入校验——若冻结资料含逐场景 SL 路径边界与 ST 走廊几何，则最小间距可由 l(s) 与障碍物投影后处理反算，属派生分析非重跑仿真，可低成本闭合全文最重结论的安全缺口。需人工核实冻结资料是否含逐场景 SL 几何。
3. QP 墙钟耗时 N 次重复 + 方差/中位数。需新实验。
4. 灵敏度分布刻画（mean±std / 逐场景翻转率表）+ 场景四 57% 翻转样本通行结果重跑确证。逐样本统计需人工核实样本可得性；翻转样本重跑属新实验。
5. 感知/预测噪声灵敏度（预测误差→时变边界→通行带传播）。需新实验。

> 跨视角提醒（已落实于 M5-1）：apollo 审已确认 11.0 SpeedBoundsDecider 纵向下界为不后退型、不存在 OVERTAKE 诱导的强制正下界，支撑 theory M5-1 取「其一」处置。

---

## 一句话小结

本轮 4 major（theory M5-1/M5-2、writing M5-1/M5-2）+ 12 minor（apollo A-2/A-3/A-4、method 1/2/3/4、writing m5-2、theory m5-1/m5-2/m5-3/m5-4）共 16 条已逐条小段 Edit 落实并 grep 核实；writing m5-4 随摘要改动一并修。压下 2 条（writing m5-1 长句、m5-3 bib 清理，均留待英文转换/定稿阶段）。5 项需新实验/需人工仅登记。编译通过。
