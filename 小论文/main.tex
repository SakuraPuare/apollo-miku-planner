% =====================================================================
% MIKU SCI 小论文 — 中文稿
% 自包含可编译骨架，编译命令： latexmk -xelatex main.tex
% =====================================================================
\documentclass[a4paper,zihao=5]{ctexart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{geometry}
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc, patterns, decorations.pathreplacing}
\usepackage{caption}
\captionsetup{font=small,skip=4pt}
\usepackage{subcaption}
\usepackage{float}
\usepackage{adjustbox}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{fillbetween}
\pgfplotsset{table/search path={../图片, ../图片/data}}
\providecommand{\datapath}{../图片/data/01_crossing_ped/}
% 概念图内部引用的示例参数宏，给出缺省值以保证自包含编译
\input{_ped_macros}
% ---- 图片目录与统一配色（复用毕设 图片/ 下自包含 TikZ 概念图）----
\graphicspath{{../图片/}}
\makeatletter
\def\input@figpath#1{\input{../图片/#1}}
\makeatother
\definecolor{deepblue}{RGB}{81, 132, 178}
\definecolor{lightblue}{RGB}{170, 212, 248}
\definecolor{skyblue}{RGB}{154, 220, 255}
\definecolor{inputyellow}{RGB}{255, 248, 154}
\definecolor{outputpink}{RGB}{255, 178, 166}
\definecolor{improvedpink}{RGB}{255, 138, 174}
\definecolor{lightpink}{RGB}{241, 167, 181}
\definecolor{deeppink}{RGB}{213, 82, 118}
\definecolor{bglight}{RGB}{242, 245, 250}
\definecolor{lightgreen}{RGB}{180, 230, 180}
\definecolor{lightorange}{RGB}{255, 210, 140}
\definecolor{dangerred}{RGB}{230, 100, 100}
\definecolor{vibrantgreen}{RGB}{46, 175, 80}
\definecolor{vibrantorange}{RGB}{240, 130, 30}
\definecolor{vibrantred}{RGB}{220, 50, 50}
\tikzset{
  mod/.style            = {rectangle, rounded corners=2pt, draw=deepblue, fill=lightblue, minimum height=0.9cm, align=center, font=\small},
  modimp/.style         = {rectangle, rounded corners=2pt, draw=deeppink, fill=improvedpink, minimum height=0.9cm, align=center, font=\small},
  modin/.style          = {rectangle, draw=deepblue, fill=inputyellow, minimum height=0.9cm, align=center, font=\small},
  modout/.style         = {rectangle, draw=deeppink, fill=outputpink, minimum height=0.9cm, align=center, font=\small},
  axx/.style            = {->, thick, black!80},
  flow/.style           = {-Stealth, thick, deepblue},
  flowimp/.style        = {-Stealth, thick, deeppink},
  obsstatic/.style      = {fill=dangerred!70, draw=dangerred, thick},
  obsdyn/.style         = {fill=lightorange, draw=dangerred, thick},
  obsbuf/.style         = {draw=dangerred!70, dashed, thick},
  bandok/.style         = {fill=lightgreen, opacity=0.55},
  bandbad/.style        = {fill=dangerred!30, opacity=0.55},
  egopt/.style          = {fill=deepblue, draw=deepblue},
  reflinestyle/.style   = {deepblue, very thick},
}
\usepackage[backend=biber,style=gb7714-2015,gbpub=false,
            uniquename=init,uniquelist=true,
            maxcitenames=2,mincitenames=1,
            sorting=none,doi=false,url=false,
            natbib=true]{biblatex}

\geometry{margin=2.2cm}
\ctexset{
  section/beforeskip   = 2.2ex plus .5ex minus .2ex,
  section/afterskip    = 1.6ex plus .3ex,
  subsection/beforeskip= 1.8ex plus .4ex minus .2ex,
  subsection/afterskip = 1.2ex plus .3ex,
}

% ---- 定理类环境（中文，按节编号）----
\theoremstyle{definition}
\newtheorem{proposition}{命题}[section]
\newtheorem{lemma}{引理}[section]
\newtheorem{theorem}{定理}[section]
\renewcommand{\proofname}{证明}

% ---- 行距与列表 ----
\linespread{1.02}
\setlist{nosep}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\setlength{\abovedisplayskip}{5pt plus 1pt minus 1pt}
\setlength{\belowdisplayskip}{5pt plus 1pt minus 1pt}
\emergencystretch=3em  % 容纳无法断行的长等宽标识符，避免 overfull

% ---- 参考文献 ----
\addbibresource{references.bib}
\renewcommand*{\bibfont}{\footnotesize}
\setlength{\bibitemsep}{1.5pt}

\title{MIKU：面向路径--速度解耦规划器的\\多障碍物时空同伦约束统一方法}
\author{廖嘉旺，孙成娇\thanks{通讯作者。}\\[2pt]\normalsize 湖北文理学院 计算机工程学院}
\date{}

\begin{document}
\maketitle

\begin{abstract}
\small
\noindent
路径--速度解耦规划器在动态多障碍物环境中面临两项关键挑战：路径阶段缺少到达时序会造成动态占据的过度压缩，逐障碍物避让则容易产生局部选向冲突。为此，本文提出多障碍物交互感知运动学约束统一方法（Multi-Obstacle Interaction-Aware Kinematic Constraint Unification, MIKU），在路径--速度解耦规划链中建立从交互风险评估、时变占据投影、组级空间决策到 ST 时间同伦的完整约束构造框架，并在 Apollo 环境中完成工程集成。MIKU 通过多因子威胁度生成差异化安全裕度，利用预计到达时间 $\tau(s)$ 查询动态障碍物占据，并以扫描线组织纵向连通分量；在统一的自车中心边界语义下，组内绕行组合可转化为连续分割和最大间隙求解，使单截面搜索由 $O(2^k)$ 降为 $O(k\log k)$，再通过 Top-K 空间同伦选择连续通行带。动态占用补集产生的先通过/后通过安全窗经多障碍物时间同伦图选择后写入 ST 位置上下边界，从而保持分段加加速度路径 QP 与速度 QP 的目标函数、运动学等式和求解器结构不变。在 4,000 组含 0--8 个禁行区间的随机实例中，最大间隙扫描解与 $2^k$ 穷举最优解逐例一致。在覆盖 7 类交通构型的 3,500 个配对随机场景中，MIKU 的无碰撞终点到达率为 77.5\%，较时间盲基线的 62.7\% 提高 14.8 个百分点（95\% 配对 bootstrap 置信区间为 $[13.5,16.0]$ 个百分点）；碰撞率由 2.2\% 降至 1.1\%，平均进度率提高 8.9 个百分点，jerk RMS 降低 0.120\,m/s$^3$，规划耗时中位数保持在约 10\,ms。结果表明，MIKU 有效缓解了动态投影保守性和多障碍物局部选向冲突，并以较低集成代价提升解耦规划器的通行能力、轨迹安全性与平顺性。

\vspace{0.5em}
\noindent\textbf{关键词：}自动驾驶；路径--速度解耦；时变通行带；多障碍物决策；组合优化
\end{abstract}
% ====== 1 引言（文献综述驱动） ======
\section{引言}
\label{sec:intro}

轨迹规划需要在动态环境中同时处理空间可通行性和到达时序。路径--速度解耦（Path-Velocity Decomposition, PVD）把两者分成 Frenet 空间的横向路径与固定路径上的纵向速度，因而适合在线规划\citep{kant1986pvd,werling2010,zhou2021dliaps}，也被 Apollo 等工程规划器采用\citep{fan2018baidu,apollo2024,zhou2021pathqp,wang2022_jfr}。但在路径边界构造中，动态障碍物常被简化为当前占据或预测时域的并集，多个障碍物又按局部规则依次选择绕行方向。前者会把本不同时发生的占据叠加，后者会遗漏组内其他可行选向，两种机制都可能让中心可行区间过度收缩\citep{meng2019_access,gonzalez2016,paden2016}。

现有方法大致可分为三类。第一类在 SLT 或其他时空表示中联合搜索与优化，能直接协调几何和时序，但需要更换或扩展原解耦求解链\citep{mcnaughton2011lattice,yang2021_ia_planner,hu2023_hybrid_astar_slt,hu2025_dp_nmpc,qiao2025stjoint}。第二类保留路径--速度分解，通过迭代、候选配对或软代价增强阶段间协调\citep{liu2017speed,chen2019em,zhang2023_dp_rcg,han2026_contingency,pathspeedstructured2024}。混合整数规划可把离散避让方向与连续轨迹统一求解，但其实现与计算取舍不同于现有两阶段 QP\citep{kessler2023}。第三类先构造 Frenet 或时空安全走廊，再在走廊内生成轨迹\citep{liu2017sfc,yoon2024stcorridor,frenetcorridor2025}；这些研究表明显式可行域是将离散决策与连续优化衔接的有效中间表示。近年局部规划研究还强调预测的动态性与交互性\citep{schwarting2018planning,yangbo2023ped,jeong2021ml}。

\begin{table}[t]
\centering
\caption{MIKU 与相关规划体系的结构对比}
\label{tab:related_compare}
\small
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{llll}
\toprule
\textbf{方法体系} & \textbf{时空协调方式} & \textbf{多障碍物空间决策} & \textbf{与两阶段 QP 的关系} \\
\midrule
联合 SLT 搜索/优化\citep{hu2023_hybrid_astar_slt,hu2025_dp_nmpc,qiao2025stjoint} & 联合变量中直接协调 & 搜索或联合优化 & 扩展求解链 \\
迭代/候选式 PVD\citep{chen2019em,zhang2023_dp_rcg} & 多轮更新或候选配对 & 随迭代或候选生成 & 多次调用 \\
时空安全走廊\citep{liu2017sfc,yoon2024stcorridor} & 显式走廊约束 & 依赖走廊拓扑 & 走廊内求解 \\
Apollo 解耦基线\citep{fan2018baidu,apollo2024} & 动态冲突主要进入 ST & 逐障碍物边界更新 & 原生保留 \\
MIKU（本文） & 时变 SL 投影与 ST 时间同伦 & 精确分割扫描 $+$ Top-K 空间同伦 & 原生保留 \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

表\ref{tab:related_compare}进一步给出相关体系在求解结构上的差异。本文聚焦于解耦规划中的约束构造与阶段协调：在保持原路径 QP 和速度 QP 高效求解结构的同时，使路径边界同时反映障碍物的预计到达时刻、多障碍物绕行组合及其对应的速度时序条件。该问题位于离散决策与连续优化之间，直接决定了解耦规划器能否把动态环境中的可通行空间稳定地传递给后续轨迹优化。


\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_greedy_vs_global_a}}
  \caption{逐障碍物选向产生虚假阻塞}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_greedy_vs_global_b}}
  \caption{组级协同选向恢复通行带}
\end{subfigure}
\caption{本文处理的多障碍物空间选向冲突}
\caption*{逐个不可回退的选向会同时压缩路径上下界；将组内方向联合成连续分割后，可直接保留有效间隙。}
\label{fig:q34_teaser}
\end{figure}

实验以 7 类交通构型的大样本配对对照为主，并以 8 个确定性场景展示动态投影、差异化裕度和组级选向的典型作用过程。减法消融进一步定位各组成机制的作用，权重扰动实验检验差异化安全边界的数值稳定性。

% ====== 2 问题提出 ======
\section{问题描述与形式化}
\label{sec:problem}

路径--速度解耦把三维时空轨迹规划降为两个顺序求解的二维子问题：先在纵向--横向（station--lateral, SL）空间规划横向路径 $l(s)$，再沿该路径在纵向--时间（station--time, ST）空间规划速度曲线 $s(t)$。本节先形式化两个凸子问题及其环境约束接口，再统一道路和障碍物的自车中心边界语义，进而说明时间无关投影与逐障碍物选向如何分别收缩可行域。

\subsection{路径--速度解耦求解主干与约束接口}
\label{subsec:decouple_formalize}

通用路径--速度解耦主干在空间采样点 $s_j$ 上优化路径状态 $\mathbf x_p=(l_j,l'_j,l''_j)_{j=0}^{K}$，再在时间采样点 $t_r$ 上优化速度状态 $\mathbf x_v=(s_r,v_r,a_r)_{r=0}^{N}$。分段加加速度路径 QP 与速度 QP 可统一写为\citep{fan2018baidu,zhou2021pathqp}
\begin{align}
 \mathbf x_p^*={}&\arg\min_{\mathbf x_p}
 \frac{1}{2}\mathbf x_p^{\mathsf T}H_p\mathbf x_p+q_p^{\mathsf T}\mathbf x_p,
 &\mathrm{s.t.}\quad&A_p\mathbf x_p=b_p,\quad l_j^-\leq l_j\leq l_j^+, \label{eq:path_qp}\\
 \mathbf x_v^*={}&\arg\min_{\mathbf x_v}
 \frac{1}{2}\mathbf x_v^{\mathsf T}H_v\mathbf x_v+q_v^{\mathsf T}\mathbf x_v,
 &\mathrm{s.t.}\quad&A_v\mathbf x_v=b_v,\quad s_r^{lb}\leq s_r\leq s_r^{ub}, \notag\\
 &&&0\leq v_r\leq v_{max},\quad a_{min}\leq a_r\leq a_{max}. \label{eq:speed_qp}
\end{align}
其中 $H_p$、$H_v$ 分别编码横向偏移、导数、加速度及 jerk 等平滑代价，$A_p$、$A_v$ 编码相邻采样点间的分段加加速度运动学等式。路径边界 $\mathcal B_l=\{[l_j^-,l_j^+]\}$ 与 ST 位置边界 $\mathcal B_s=\{[s_r^{lb},s_r^{ub}]\}$ 是两个凸 QP 接收环境约束的核心接口。MIKU 保留式\eqref{eq:path_qp}--\eqref{eq:speed_qp}的目标矩阵和运动学等式，统一重构这两组边界：
\begin{equation}
 (\mathcal B_l,\mathcal H_s)=\Phi_{\mathrm{MIKU}}(\mathcal R,\mathcal X_{ego},\{O_i(t)\}),
 \qquad
 \mathcal H_t=\Phi_{\mathrm{time}}(l(s),\{O_i(t)\}),\quad
 \mathcal B_s=\Phi_{\mathrm{ST}}(l(s),\{O_i(t)\})\cap\Phi_{\mathcal H_t}(\mathcal H_t),
 \label{eq:miku_interface}
\end{equation}
其中 $\mathcal R$ 为道路与参考线信息，$\mathcal X_{ego}$ 为自车状态，$\mathcal H_s$、$\mathcal H_t$ 分别为选定的空间与时间同伦；$\Phi_{\mathrm{ST}}$ 表示基础解耦规划器的路径相关 ST 映射，$\Phi_{\mathcal H_t}$ 表示 MIKU 将时间同伦写入 ST 上下边界的跨阶段传递。式\eqref{eq:miku_interface}把环境预测、空间选向和速度时序汇入同一解耦求解链，构成本文完整算法的数学接口。

在 Frenet 坐标系下，第 $i$ 个障碍物的物理投影记为 $O_i=[s_i^{-},s_i^{+}]\times[l_i^{-},l_i^{+}]$，道路物理横向边界记为 $[l_{road}^{-},l_{road}^{+}]$。设自车宽度为 $W_{ego}$，路侧额外裕度为 $d_{road}$，则自车中心可行道路区间为
\begin{equation}
    [L^{-},L^{+}]=
    [l_{road}^{-}+W_{ego}/2+d_{road},\;l_{road}^{+}-W_{ego}/2-d_{road}].
    \label{eq:center_road}
\end{equation}
障碍物物理边界同样只外扩一次半车宽与障碍物额外裕度 $d_{buf,i}$，得到针对自车中心的禁行区间
\begin{equation}
    [u_i,v_i]=[l_i^{-}-W_{ego}/2-d_{buf,i},\;l_i^{+}+W_{ego}/2+d_{buf,i}].
    \label{eq:center_forbidden}
\end{equation}
因而 $L^{-}$、$L^{+}$、$u_i$、$v_i$ 全部是自车中心的坐标边界，下游间隙宽度不得再减一次车宽。

为每个在截面 $s$ 处活跃的障碍物分配二值避让决策 $d_i\in\{L,R\}$，记两类集合为 $\mathcal L$ 和 $\mathcal R$，则中心通行带为
\begin{equation}
    l^{-}(\mathbf d)=\max\!\left(L^{-},\max_{i\in\mathcal L}v_i\right),\qquad
    l^{+}(\mathbf d)=\min\!\left(L^{+},\min_{i\in\mathcal R}u_i\right).
    \label{eq:center_band}
\end{equation}
其宽度 $W(\mathbf d)=l^{+}(\mathbf d)-l^{-}(\mathbf d)$。给定数值容差 $\varepsilon>0$，$W(\mathbf d)\geq\varepsilon$ 即表示存在可用的自车中心区间；不再以 $W(\mathbf d)\geq W_{ego}$ 重复判定整车宽度。

真实场景中障碍物随时间运动。引入自车到达纵向位置 $s$ 的预估时刻 $\tau(s)$，障碍物投影随之成为 $O_i(\tau(s))$，禁行边界按式\eqref{eq:center_forbidden}同步更新。$\tau(s)$ 由上一规划周期的速度曲线或二阶运动学模型估计，并在每个滚动规划周期更新。对包含 $k$ 个障碍物的纵向连通分量 $C$，记障碍物 $i$ 的代表纵向位置为 $\bar s_i$，在 $\bar t_i=\tau(\bar s_i)$ 处查询其禁行区间 $[u_i(\bar t_i),v_i(\bar t_i)]$。基于这组时变代表区间，组级选向问题写为
\begin{equation}
    \mathbf{d}_{C}^{*} = \arg\max_{\mathbf{d}_{C}\in\{L,R\}^{k}}
    \left[\min\!\left(L^{+},\min_{i\in\mathcal R_C}u_i(\bar t_i)\right)
    -\max\!\left(L^{-},\max_{i\in\mathcal L_C}v_i(\bar t_i)\right)\right],
    \label{eq:prob_joint}
\end{equation}
其中 $\mathbf d_C=(d_1,\ldots,d_k)$ 为组内绕行方向，$\mathcal L_C$、$\mathcal R_C$ 为对应的方向分组。逐项枚举需要检查 $2^k$ 种组合；不同纵向连通分量相互独立后，整条路径的离散决策被分解为各组问题之和。第\ref{sec:method_band}节将证明式\eqref{eq:prob_joint}存在连续分割最优解，并以 $O(k\log k)$ 的排序与扫描精确求得代表区间集合的最大中心间隙；所得组级方向随后逐截面写回时变路径边界。

MIKU 沿用路径--速度顺序求解结构，并重新组织两个阶段之间的约束信息：路径阶段根据 $\tau(s)$ 构造时变通行带，速度阶段接收该通行带成立所需的时序边界。由此，动态预测不再停留在速度层，而是前移到路径边界生成过程，并通过 ST 约束传递形成完整闭环。

\subsection{时序约束压缩与失效机理}
\label{subsec:union_projection}

路径子问题忽略时间时，需要用时间无关区域近似动态占据。一种常见的保守选择是在预测时域 $[0, T_{\text{pred}}]$ 上取全时刻并集投影
\begin{equation}
    \bar{O}_i = \bigcup_{t \in [0,\,T_{\text{pred}}]} O_i(t),
    \label{eq:prob_union}
\end{equation}
式~\eqref{eq:prob_union} 把“障碍物在何时占据何处”压缩为“障碍物曾经占据何处”；另一类实现只取 $t{=}0$ 快照，或暂不让动态障碍物参与路径边界构造。三种处理虽然实现不同，但路径阶段均不能沿自车到达时间查询预测轨迹。图~\ref{fig:dynamic_exclusion} 给出横穿行人的真实时序：预测初始时行人尚未进入路径，随后横穿车道，而自车抵达相同纵向位置时，行人已横移至另一侧并打开通行间隙。时间无关投影仍会把整段扫掠区域视为同时占据，如图~\ref{fig:baseline_stopband} 所示，进而把可通行的时序窗口压缩为停止约束，直到下一周期观察到障碍物离开后才重新放行。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.30\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_dynamic_exclusion_a}}
  \caption{$t{=}0$ 行人未进入路径}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.30\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_dynamic_exclusion_b}}
  \caption{中间时刻行人半入路径}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.30\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_dynamic_exclusion_c}}
  \caption{自车到达时通行带打开}
\end{subfigure}
\caption{横穿行人场景下的时序约束缺失}
\caption*{全时刻并集投影把行人扫过的整段车道并入静止足迹，路径层据此判定阻塞；而沿到达时间 $\tau(s)$ 的时变投影只取自车实际到达时刻的占用，通行带早已打开。}
\label{fig:dynamic_exclusion}
\end{figure}

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.46\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_baseline_stopband_a}}
  \caption{预测到行人冲突即生成停止带}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.46\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_baseline_stopband_b}}
  \caption{行人物理通过后停止带转通行带}
\end{subfigure}
\caption{时间盲解耦基线的速度阶段等待}
\caption*{本文基线的路径阶段不按预计到达时刻查询动态行人，因而保持参考线路径；冲突随后在 ST 映射与速度阶段形成红色停止带，浅黄色区域表示安全裕度。直到后续预测给出可通过时序，速度阶段才放行，未利用图~\ref{fig:dynamic_exclusion} 所示的时变横向间隙。}
\label{fig:baseline_stopband}
\end{figure}

时间无关投影把动态障碍物的二维时变占据压缩到固定的横向约束中，使路径阶段无法区分“当前被占据”与“自车到达时仍被占据”。若路径阶段暂不处理动态障碍物，冲突又会全部推迟到速度规划中解决，容易形成直线路径上的集中制动。MIKU 以 $\tau(s)$ 为索引恢复占据的时间条件，在路径层提前形成与预测运动一致的横向通行带，并把相应到达条件继续传给速度层，从而同时利用空间绕行和速度调整两类自由度。

路径可行域的收缩来自两个可分别观察的机制：时间维度上的占据压缩或延迟处理，以及空间维度上的逐障碍物贪心选向。传统贪心方法按照障碍物中心与参考路径的相对位置依次选择绕行方向，已作出的选择不再回退；若冲突的上下边界更新使某截面满足 $l^{+}(s)<l^{-}(s)$，路径子问题便返回不可行并转入停车降级。该过程在 $2^k$ 种方向组合中只检查一种，可能选中分裂式组合而遗漏其余可行分割。MIKU 在 $\tau(s)$ 对应的动态截面上统一构造中心禁行区间，并以连续分割遍历全部有效间隙，使时间有效性与空间组合在同一通行带生成过程中得到协调。

时间无关投影的一个直接边界情形是：若某纵向区间内，障碍物在预测时域上的膨胀并集完全覆盖式\eqref{eq:center_road}的中心可行道路区间，则时间盲路径子问题在该段上必然无可行中心点；沿 $\tau(s)$ 查询的瞬时占据则可能已经打开通道。空间组合部分进一步由第\ref{sec:method}节的连续分割引理和最大间隙定理给出精确判据，使算法能够区分真实阻塞与贪心选向造成的虚假不可行。

基于上述分析，MIKU 将问题组织为五个相互衔接的环节：交互感知安全裕度、到达时间驱动的动态投影、连通分量内的精确间隙生成、分量间的 Top-K 空间同伦选择，以及占用补集向 ST 上下边界的时间同伦传递。

% ====== 3 MIKU 算法（方法 + 理论） ======
\section{MIKU 多障碍物时空同伦约束统一方法}
\label{sec:method}

第\ref{sec:problem}节给出了统一的中心边界语义。MIKU 首先在预测占据上构造交互感知的空间与时间候选，再将离散同伦编译为路径边界与 ST 位置边界，最后由连续凸 QP 生成平滑轨迹。每个规划周期先估计 $\tau(s)$ 并查询时变 SL 投影，再按纵向重叠关系分组，通过精确间隙扫描和 Top-K 空间同伦生成连续路径边界；路径求解后，沿 $l(s)$ 构造预测占用管与安全时间窗，利用分层时间同伦图选择全局因果序列并传递为 ST 上下边界。这条候选--约束--连续求解主链如图~\ref{fig:miku_method_flow}所示。

MIKU 由交互感知安全边界、到达时间驱动的时变投影、精确间隙扫描、Top-K 空间同伦、预测不确定性占用管与多障碍物时间同伦共同组成。这些模块依次回答“约束应扩张多少”“多障碍物从何处连续通过”“预测误差如何进入占用边界”以及“先通过/后通过选择如何进入速度规划”。图~\ref{fig:miku_method_flow}先展示与平台无关的算法信息流，算法~\ref{alg:miku}再给出可执行步骤。

\begin{figure}[htbp]
\centering
\adjustbox{max width=\textwidth}{\input{../图片/fig_q34_miku_framework}}
\caption{MIKU 的平台无关算法主链}
\caption*{红色模块表示 MIKU 的候选与约束构造，蓝色模块表示通用的路径/速度连续求解器。算法先以空间同伦生成路径边界，再以时间同伦将通行时序写入 ST，速度解反馈到下一规划周期。}
\label{fig:miku_method_flow}
\end{figure}

在具体工程平台中，上述连续求解可由既有分段加加速度路径 QP 与速度 QP 实现。本文在第~\ref{subsec:exp_setup}节中以 Apollo 作为集成平台说明边界接口，不改变上述算法定义。

\subsection{交互感知威胁度与差异化安全裕度}
\label{sec:method_threat}

统一的额外缓冲在窄路两侧同时受限时容易过度挤压净间距。MIKU 使用归一化威胁度 $\Theta_i$ 为不同障碍物生成差异化安全裕度 $d_{buf,i}$，使边界扩张同时反映碰撞紧迫性、横向重叠、相对运动、目标类型和局部交互密度。

MIKU 为每个障碍物定义归一化威胁度 $\Theta_i$，综合 5 个因子加权得到

\begin{equation}
    \Theta_i = w_1 f_{TTC}(i) + w_2 f_{overlap}(i) + w_3 f_{vel}(i) + w_4 f_{type}(i) + w_5 f_{inter}(i),
    \label{eq:threat_score}
\end{equation}

其中各因子归一化至 $[0,1]$，权重之和为一。本文取 $(w_1,w_2,w_3,w_4,w_5)=(0.30,0.20,0.15,0.10,0.25)$，依次体现碰撞紧迫性、横向重叠、相对速度、目标类型和局部密度在边界扩张中的优先级，其参数稳定性在第\ref{subsec:exp_sensitivity}节验证。

5 个因子各刻画一种风险来源。碰撞时间（Time To Collision, TTC）因子衡量纵向碰撞的紧迫性，当 $s_i > s_{ego}$ 且 $\dot{s}_{ego} > \dot{s}_i$ 时 $\mathrm{TTC}_i = (s_i - s_{ego})/(\dot{s}_{ego} - \dot{s}_i)$，障碍物位于自车后方、自车不会追上或相对速度为零等退化情形均令 $\mathrm{TTC}_i=+\infty$，不构成纵向威胁；再以分段线性映射把碰撞时间转为威胁度，$\mathrm{TTC}_i \leq T_{crit}$ 时 $f_{TTC}=1$，$\mathrm{TTC}_i \geq T_{max}$ 时 $f_{TTC}=0$，其间取 $f_{TTC}(i) = (T_{max} - \mathrm{TTC}_i)/(T_{max} - T_{crit})$ 线性递减。临界值取 $T_{crit}=2.0$\,s，依据 Minderhoud 与 Bovy 对碰撞时间分布的分析\citep{minderhoud2001extended}，碰撞时间低于该值时驾驶员已难以靠转向规避；上限取 $T_{max}=7.0$\,s，以覆盖本文采用的速度规划时间域。

其余四个因子分别从横向、速度、类型与密集度补全风险画像。横向重叠率因子 $f_{overlap}(i)=\max\!\big(0,\ \min(l_i^{+}, l_{ego}^{+}) - \max(l_i^{-}, l_{ego}^{-})\big)/W_{ego}$ 度量障碍物与自车走廊的横向重叠比例，$[l_{ego}^{-}, l_{ego}^{+}]$ 为自车当前占据的横向区间，零为完全错开、一为正对自车。相对速度因子 $f_{vel}(i)=\sigma(v_{rel,i}/v_{max})$ 取 $\sigma(x)=1/(1+e^{-5x})$ 的 Sigmoid 映射，$v_{rel,i}=\dot{s}_{ego}-\dot{s}_i$，$v_{max}$ 为速度归一化尺度，相对速度过零附近的区分比极高速度段更重要，故不取线性。类型因子 $f_{type}(i)$ 按保护优先级取离散值：行人、骑行者等弱势道路使用者（Vulnerable Road User, VRU）取 $1.0$，机动车取 $0.7$，未知可动目标取 $0.5$，静态障碍物取 $0.3$，交通锥等小尺寸标识物取 $0.15$。交互密度因子 $f_{inter}(i) = \frac{1}{N-1} \sum_{j \neq i} \mathbf{1}[d_{ij} < d_{cluster}] \,(d_{cluster} - d_{ij})/d_{cluster}$ 以 SL 平面欧氏距离 $d_{ij}$ 与聚类半径 $d_{cluster}=10$\,m 加权统计邻近障碍物的聚集程度，孤立障碍物 $f_{inter}\approx 0$，密集车群中接近 $1$，$N=1$ 时令 $f_{inter}=0$。交互密度高的障碍物通常落入同一连通分量，将在第\ref{sec:method_band}节的纵向重叠分组中被统一协调。

威胁度确定后，额外缓冲在区间 $[d_{buf,min},d_{buf,max}]$ 上线性插值：

\begin{equation}
    d_{buf,i} = d_{buf,min} + (d_{buf,max} - d_{buf,min})\,\Theta_i,
    \label{eq:dynamic_delta}
\end{equation}

额外缓冲区间取 $[0.10,0.40]$\,m。半车宽 $W_{ego}/2$ 在式\eqref{eq:center_forbidden}中始终保留，式\eqref{eq:dynamic_delta}只改变障碍物外侧的附加空间，从而避免在后续间隙判定中重复处理车宽。代入式\eqref{eq:center_forbidden}得

\begin{equation}
    u_i=l_i^{-}-W_{ego}/2-d_{buf,i},\qquad
    v_i=l_i^{+}+W_{ego}/2+d_{buf,i},
    \label{eq:dynamic_uv}
\end{equation}

其中 $u_i$、$v_i$ 已是自车中心约束。$d_{buf,i}$ 只改变边界取值，不改变第\ref{sec:method_band}节的排序与扫描结构。图~\ref{fig:threat_radar}展示该参数化效果。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.42\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_threat_radar_a}}
  \caption{高威胁横穿行人}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.42\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_threat_radar_b}}
  \caption{低威胁远离静态目标}
\end{subfigure}
\caption{多因子威胁度画像与差异化安全裕度}
\caption*{五个因子归一化后合成威胁度 $\Theta$，横穿行人在碰撞时间、类型与横向重叠上均高，$\Theta{=}0.82$ 得接近上限的安全裕度；远离的静态目标各因子均低，$\Theta{=}0.18$ 将额外缓冲压至下限，把被无谓占用的横向空间归还通行带。}
\label{fig:threat_radar}
\end{figure}

\subsection{扫描线分组与组级最大间隙通行带}
\label{sec:method_band}

确定每个障碍物的安全边界后，第二个问题是把局部约束合成为全局协调的横向通行带。传统逐障碍物贪心方法依次选择左绕或右绕，当前选择不联合考虑同一 $s$ 截面的其余活跃障碍物；对称或密集配置中，不同局部选择可能分别压低上界、抬高下界，最终造成 $l^{+}(s)<l^{-}(s)$ 的虚假不可行。MIKU 将单截面决策改写为安全边界排序后的间隙选择，并先按纵向重叠关系分组，使彼此无关的障碍物独立求解、同组障碍物联合决策。

两个障碍物 $O_i$ 与 $O_j$ 称为纵向重叠，当且仅当各自纵向占据区间满足 $[s_i^{-}, s_i^{+}] \cap [s_j^{-}, s_j^{+}] \neq \varnothing$。该关系对称但不传递。取其传递闭包，把 $n$ 个障碍物划为 $m$ 个连通分量 $C_1, \ldots, C_m$；在分组所用的纵向占据语义下，不同分量的区间并集互不重叠，组级选向可独立求解，写回路径边界时再合并车辆纵向尺寸扩张后的有效约束。分组用一次扫描线完成，按 $s_i^{-}$ 升序排列后维护当前分量已见的最大 $s^{+}$ 记为 $s_{max}$，若下一障碍物的 $s^{-}$ 不超过 $s_{max}$ 则并入当前分量并更新 $s_{max}$，否则另起新分量。MIKU 直接采用纵向区间重叠作为拓扑关联判据，使分组由障碍物几何关系确定。排序为 $O(n\log n)$，扫描本身 $O(n)$，分组总体 $O(n\log n)$。

连通分量中的成员可以在不同截面依次活跃。MIKU 以组级决策统一障碍物的绕行侧，写回路径边界时则在每个 $s$ 处仅由当前活跃障碍物更新上下界。该方向一致性约束避免链式分量中的通行带频繁换侧，使离散边界保持连续、稳定。记分量 $C_j$ 在各活跃截面上生成的最小中心带宽度为 $\hat W(C_j)$，则整条离散路径的最小宽度为

\begin{equation}
    \hat{W} = \min_{j=1,\ldots,m} \hat{W}(C_j).
    \label{eq:global_width}
\end{equation}

在单个截面内，最优指在 $2^k$ 个方向组合中最大化式\eqref{eq:center_band}的中心带宽度。下述连续分割引理与最大间隙定理构成组级决策的理论基础。

\begin{lemma}[最优分割的连续性]
\label{lem:continuous_partition}
将障碍物按 $u_i$ 值升序排列后，存在最优解 $\mathbf{d}^{*}$，使得分割在排序中是连续的，即存在 $p \in \{0, 1, \ldots, k\}$ 满足
\begin{equation}
    d_{(i)}^{*} = L,\ \forall\, i \leq p; \qquad d_{(i)}^{*} = R,\ \forall\, i > p.
    \label{eq:continuous_partition}
\end{equation}
\end{lemma}

证明。两侧有一为空时取 $p=k$ 或 $p=0$。两侧均非空时令 $\beta=\min_{i\in\mathcal R}u_i$，将所有 $u_i\geq\beta$ 且原属 $\mathcal L$ 的障碍物改入 $\mathcal R$。新成员的 $u_i$ 不低于 $\beta$，故式\eqref{eq:center_band}的上界不变；$\mathcal L$ 只减少元素，下界不增，因而宽度不减。对并列 $u_i$ 用 $v_i$ 降序作二级排序，即可得到式\eqref{eq:continuous_partition}的连续分割最优解。$\hfill\Box$

引理 \ref{lem:continuous_partition} 把搜索空间由 $2^k$ 个任意分配降为 $k+1$ 个候选分割点 $p = 0, 1, \ldots, k$。其中 $p=0$ 表示全部右绕即自车走最右侧通道，$p=k$ 表示全部左绕即自车走最左侧通道，$1 \leq p \leq k-1$ 表示自车从第 $p$ 与第 $p+1$ 个障碍物之间的间隙通过。引理仅依赖 $u$ 值的排序性，不要求 $d_{buf,i}$ 为常数；当存在 $u_i$ 相等的障碍物时，可取 $v_i$ 降序为二级排序键以保证排序唯一，不影响结论。

与中心道路区间 $[L^-,L^+]$ 完全不相交的禁行区间不会收紧当前截面，先从活跃列表中删除。其余区间允许越过某一侧道路边界，并由下式中的 $\min$ 和 $\max$ 自动完成道路裁剪。对分割点 $p$，由于按 $u_i$ 排序不能保证 $v_i$ 单调，定义包含道路下界的前缀最大边界
\begin{equation}
    V_0=L^{-},\qquad V_p=\max\!\left(L^{-},\max_{1\leq q\leq p}v_{(q)}\right),
    \label{eq:prefix_v}
\end{equation}
并定义候选上界 $U_p$ 与 $k+1$ 个中心间隙
\begin{equation}
    U_p=\begin{cases}
        \min(L^{+},u_{(p+1)}), & p=0,\ldots,k-1,\\
        L^{+}, & p=k,
    \end{cases}
    \qquad g_p=U_p-V_p,
    \label{eq:gap_def}
\end{equation}
其中 $V_p$ 必须使用前 $p$ 个 $v$ 的前缀最大值；用相邻的 $v_{(p)}$ 代替会在禁行区间交错或嵌套时高估中间间隙。$L^-$ 与 $L^+$ 已包含道路侧的半车宽和裕度收缩，因而 $g_p$ 与代码中的中心可行宽度完全一致。

\begin{theorem}[最大间隙最优性]
\label{thm:max_gap}
对任一分割点 $p$，式\eqref{eq:center_band}的中心通行带下界为 $V_p$、上界为 $U_p$，故宽度 $W(p)=g_p$，且
\begin{equation}
    W^{*} = \max_{p = 0, 1, \ldots, k} g_p.
    \label{eq:max_gap_opt}
\end{equation}
\end{theorem}

证明。对分割 $p$，前 $p$ 个障碍物施加下界，式\eqref{eq:center_band}直接给出 $l^-(p)=V_p$；由于 $u_{(i)}$ 升序，后 $k-p$ 个障碍物施加的最紧上界为 $u_{(p+1)}$，同道路上界取最小得 $l^+(p)=U_p$。因此 $W(p)=U_p-V_p=g_p$。引理\ref{lem:continuous_partition}已证明至少存在一个连续分割最优解，遍历 $p=0,\ldots,k$ 后取最大 $g_p$ 即与 $2^k$ 组合枚举的最优宽度相同。$\hfill\Box$

定理\ref{thm:max_gap}中 $g_p<\varepsilon$ 表示该中心带不可用；当 $\max_p g_p<\varepsilon$ 时，才在当前中心安全边界下判定截面阻塞。由于半车宽已同时进入式\eqref{eq:center_road}和式\eqref{eq:center_forbidden}，此处不再与 $W_{ego}$ 比较。组内排序为 $O(k\log k)$，区间筛选、前缀最大值递推和候选扫描均为 $O(k)$，因而单截面组内求解为 $O(k\log k)$。第\ref{subsec:exp_exactness}节以 $2^k$ 穷举最优解为独立 oracle，对扫描实现的精确性进行大样本对照。

单组最宽间隙并不必然组成纵向上最连续的整体路径。因此，MIKU 为每个连通分量 $C_j$ 保留宽度排名前 $Q$ 的正宽候选带 $B_{jq}=[a_{jq},b_{jq}]$，其中心为 $c_{jq}=(a_{jq}+b_{jq})/2$、宽度为 $g_{jq}=b_{jq}-a_{jq}$，实现中取 $Q=3$。将纵向相邻分量视为分层图，通过动态规划选择空间同伦序列

\begin{equation}
 (q_1^*,\ldots,q_m^*)=arg\min_{q_1,\ldots,q_m}
 \left[-\sum_{j=1}^{m}g_{jq_j}
 +\lambda_s\left(|c_{1q_1}-l_0|+
 \sum_{j=2}^{m}|c_{jq_j}-c_{j-1,q_{j-1}}|\right)\right].
 \label{eq:spatial_homotopy}
\end{equation}
式\eqref{eq:spatial_homotopy}在通行带宽度与纵向换侧之间建立统一决策：节点代价奖励宽通道，边代价抑制相邻分量的带中心跳变。定义 $D_{jq}$ 为到达节点 $(j,q)$ 的最小累积代价，则递推关系为
\begin{equation}
 D_{jq}=-g_{jq}+\min_{r=1,\ldots,Q}
 \left[D_{j-1,r}+\lambda_s|c_{jq}-c_{j-1,r}|\right],
 \label{eq:spatial_dp}
\end{equation}
初层以 $D_{1q}=-g_{1q}+\lambda_s|c_{1q}-l_0|$ 初始化，末层取最小值并回溯即得式\eqref{eq:spatial_homotopy}在 Top-K 候选集上的全局最优序列。该层级保留定理\ref{thm:max_gap}给出的精确候选结构，同时避免把各组局部最宽解机械串联成频繁换侧的路径。

\begin{figure}[htbp]
\centering
\adjustbox{max width=0.8\textwidth}{\input{../图片/fig_max_gap_theorem}}
\caption{按 $u_i$ 排序后的候选间隙与最优连续分割}
\caption*{$k{=}4$ 个障碍物在排序后产生 $k{+}1{=}5$ 个候选间隙，含两个端部间隙 $g_0$、$g_4$；最大间隙 $g_2^{\star}$ 对应最优分割点 $p^{\star}{=}2$，其左侧障碍物右绕、右侧左绕，决策向量在排序中连续无交错，印证引理~\ref{lem:continuous_partition} 与定理~\ref{thm:max_gap}。}
\label{fig:max_gap}
\end{figure}

\subsection{到达时间驱动的时变投影}
\label{sec:method_corridor}

时变投影以到达时间函数 $\tau(s)$ 查询自车抵达纵向位置 $s$ 时的障碍物占据。$\tau(s)$ 作为当前路径阶段的时序输入，本文 8 场景由当前自车状态的二阶运动学模型计算；滚动规划时也可由上周期速度曲线形成查询函数。以匀加速运动学估计时

\begin{equation}
    \tau(s) = \frac{-v_{ego} + \sqrt{v_{ego}^{2} + 2\,a_{ego}\,(s - s_{ego})}}{a_{ego}},
    \label{eq:arrival_time}
\end{equation}

其中 $s_{ego}$、$v_{ego}$、$a_{ego}$ 来自当前自车状态估计。实现对退化分支作数值处理：$|a_{ego}|<10^{-3}$ 时采用 $\tau(s)=(s-s_{ego})/\max(v_{ego},10^{-3})$；判别式 $v_{ego}^{2}+2a_{ego}(s-s_{ego})<0$ 时返回超出规划时域的大时刻标记。若使用上周期单调速度曲线，则对首次满足 $s_{prev}(t)\geq s$ 的时刻作一维插值，并在每个规划周期随最新轨迹更新。

有了 $\tau(s)$，每个动态障碍物的横向占据便取自自车到达该截面时的预测位置。本文场景以常速度模型 $s_i(t)=s_{i,0}+v_{s,i}t$、$l_i(t)=l_{i,0}+v_{l,i}t$ 给出障碍物预测，再结合几何尺寸得到时变 SL 边界 $[l_i^{-}(\tau(s)), l_i^{+}(\tau(s))]$；若该时刻障碍物纵向占据不覆盖截面 $s$，则标记为不活跃。该查询形式可直接接收上游预测器输出的离散轨迹及其插值结果。第\ref{sec:method_threat}节的横向重叠率因子使用同一时变投影，静态障碍物仍取当前观测位置。

为使时变通行带在有界预测误差下仍保持几何安全性，MIKU 将障碍物中心预测扩张为不确定性占用管。设初始位置误差界为 $(\epsilon_{s0,i},\epsilon_{l0,i})$，速度误差界为 $(\epsilon_{vs,i},\epsilon_{vl,i})$，则时刻 $t$ 的纵横向扩张量为
\begin{equation}
 \epsilon_{s,i}(t)=\epsilon_{s0,i}+t\epsilon_{vs,i},\qquad
 \epsilon_{l,i}(t)=\epsilon_{l0,i}+t\epsilon_{vl,i}.
 \label{eq:prediction_tube}
\end{equation}
障碍物物理外形与式\eqref{eq:prediction_tube}、自车外形依次作 Minkowski 扩张，得到进入 ST 映射的预测占用管。因此，MIKU 使用的时间窗边界来自显式误差界下的障碍物占用，而不是由名义到达时间代替占用预测。

在每个路径离散位置 $s$，仅当障碍物在 $\tau(s)$ 时刻的纵向占据覆盖该截面时，才用当时的 $l_i^-(\tau(s))$、$l_i^+(\tau(s))$ 构造式\eqref{eq:center_forbidden}。对静态障碍物仍查询当前占据。组级分割基于成员各自代表纵向位置处的时变禁行区间联合选定，并在该分量内保持每个障碍物的绕行侧不变；写回路径边界时，$u_i(\tau(s))$、$v_i(\tau(s))$ 和活跃集合均逐截面重算。定理\ref{thm:max_gap}保证代表区间集合上的精确最大间隙，分量内的方向一致性则把该决策稳定地延伸到各活跃截面。

该过程使路径边界由固定障碍物投影转化为随到达时间更新的动态通行带，能够及时释放已经让出的横向空间，并为后续速度规划保留通行带成立所需的时间条件。

\subsection{多障碍物时间同伦与 ST 上下边界传递}
\label{sec:method_inject}

设第 $c$ 个局部冲突区的纵向范围为 $[s_c^-,s_c^+]$，预测占用管与规划路径重叠的时间区间为 $I_{cr}=[t_{cr}^{in},t_{cr}^{out}]$。将每个占用区间向两侧扩张时间安全量 $\eta_c$，并在规划时域 $[0,T]$ 内合并，则可安全到达该冲突区的时间窗集合为
\begin{equation}
 \mathcal W_c=[0,T]\setminus
 \bigcup_r[t_{cr}^{in}-\eta_c,t_{cr}^{out}+\eta_c].
 \label{eq:safe_windows}
\end{equation}
式\eqref{eq:safe_windows}自然同时产生“在障碍物进入前通过”和“在障碍物离开后通过”两类时间同伦；与自车最早可达时间相交后，得到可达窗 $W_{cq}=[\alpha_{cq},\beta_{cq}]$。$\tau(s_c)$ 只用于为候选窗排序，安全窗边界始终由预测占用的补集给出。

对多个冲突点，MIKU 按 $s_c$ 递增建立分层有向图，节点 $(c,q)$ 表示在窗 $W_{cq}$ 内通过第 $c$ 个冲突点。若相邻选择的目标到达时间 $\hat t_{c-1}$、$\hat t_c$ 满足
\begin{equation}
 \hat t_c\geq \hat t_{c-1}+\frac{s_c-s_{c-1}}{v_{max}},
 \qquad \hat t_c\in W_{cq},
 \label{eq:temporal_edge}
\end{equation}
则在两节点间连边。节点代价度量 $|\hat t_c-\tau(s_c)|$，边代价处罚先通过/后通过类别的频繁切换，滚动规划时再对偏离上周期同伦的节点加入持续性代价。对分层图执行有界宽度的动态规划，得到同时满足多个冲突点的最优时间同伦序列。

选定窗 $W_{cq}=[\alpha_{cq},\beta_{cq}]$ 后，其语义被直接写入速度 QP 的位置上下边界：
\begin{equation}
 \begin{aligned}
 s_j^{ub}&=\min\!\left(s_j^{ub,0},\ s_c^- -\epsilon_s\right),
 && t_j<\alpha_{cq},\\
 s_j^{lb}&=\max\!\left(s_j^{lb,0},\ s_c^+ +\epsilon_s\right),
 && t_j\geq\beta_{cq}.
 \end{aligned}
 \label{eq:window_to_st}
\end{equation}
当窗与时域左端或右端相接时，式\eqref{eq:window_to_st}中对应的一侧约束为空；对所有冲突点的边界取交集即得最终 $\mathcal B_s$。如无完整时间同伦，则在第一个不可达冲突点前生成停车上界。传递后仍为标准线性盒约束，因而速度 QP 的目标矩阵、运动学等式、凸性和稀疏结构全部保持不变。

\subsection{整体流程与复杂度}
\label{sec:method_algorithm}

算法\ref{alg:miku}给出从自车状态与障碍物预测轨迹到路径中心边界及速度时序约束的完整流程。

\begin{algorithm}[htbp]
\small
\caption{MIKU 多障碍物约束统一流程}
\label{alg:miku}
\KwIn{$n$ 个障碍物及预测轨迹，道路物理边界，自车状态}
\KwOut{中心路径边界 $\{[l^{-}(s),l^{+}(s)]\}$，ST 位置上下边界 $\mathcal B_s$}
按式\eqref{eq:center_road}内缩道路物理边界，初始化所有截面\;
\For{每个障碍物 $i$}{
  估计其代表截面到达时间 $\tau(s_i^-)$，查询预测占据\;
  按式\eqref{eq:dynamic_delta}计算 $d_{buf,i}$，按式\eqref{eq:center_forbidden}得 $[u_i,v_i]$\;
}
按 $s_i^-$ 升序扫描，形成纵向连通分量\;
\For{每个连通分量 $C$}{
  删除与 $[L^-,L^+]$ 完全不相交的禁行区间\;
  组内按 $(u_i,-v_i)$ 排序，递推前缀最大值 $V_p$\;
  按式\eqref{eq:gap_def}计算 $g_0,\ldots,g_k$，保留 Top-K 正宽候选带\;
}
按式\eqref{eq:spatial_homotopy}选择纵向连续的空间同伦序列\;
\For{每个选定的分量候选带}{
  固定组内绕行侧，对覆盖的每个 $s$ 查询 $\tau(s)$ 处的活跃禁行区间\;
  以式\eqref{eq:center_band}逐截面写回 $[l^-(s),l^+(s)]$\;
}
沿规划路径构造式\eqref{eq:prediction_tube}的障碍物预测占用管\;
按式\eqref{eq:safe_windows}生成各冲突点的安全时间窗\;
以式\eqref{eq:temporal_edge}连接分层时间同伦图并选择最优因果序列\;
按式\eqref{eq:window_to_st}将各选定窗与基础 ST 边界取交集\;
\KwRet 中心路径边界与 ST 位置上下边界 $\mathcal B_s$
\end{algorithm}

设路径离散点数为 $K$、纵向连通分量数为 $m$、每层保留的空间候选数为 $Q$。扫描线分组与全部组内排序的总开销为 $O(n\log n)$，单截面含 $k$ 个活跃障碍物时，候选间隙生成为 $O(k\log k)$，空间同伦动态规划为 $O(mQ^2)$。时间同伦图对每层保留至多 $B$ 个部分序列，设平均可达窗数为 $R$、冲突点数为 $h$，则搜索开销为 $O(hBR)$；本文实现取 $Q=3$、$B=8$，两者均为有界在线开销。结合空间索引和有界活跃集后，邻域查询与逐截面边界更新可线性摊还；在全部 $n$ 个障碍物均覆盖全部 $K$ 个截面的极端情况下，时变占据查询和朴素交互密度统计分别为 $O(nK)$ 与 $O(n^2)$。因此，MIKU 将原本指数级的单组方向组合转化为精确排序扫描，并以有界的空间与时间同伦搜索完成跨分量协调。

% ====== _parts/experiment.tex ======
\section{实验与分析}
\label{sec:experiment}

本节从精确求解性、随机场景统计性能、终点到达能力、轨迹平顺性、求解效率、组件作用和参数灵敏度七个方面评估 MIKU。时间盲基线与 MIKU 共享场景、车辆约束、分段加加速度路径 QP、速度 QP 与求解器，确保结果差异来自约束构造与传递机制。

\subsection{仿真平台与场景设置}
\label{subsec:exp_setup}

实验在 Apollo 11.0 路径--速度解耦规划环境中进行，运行平台为 Intel i9-14900HX 处理器、64\,GB DDR5 内存与 Arch Linux 操作系统。求解链依次包含 PathBoundsDecider 语义的 SL 边界构造、分段加加速度路径 QP、ST 边界映射、粗速度参考和分段加加速度速度 QP。

MIKU 嵌入 Apollo 原有规划链的边界接口：路径侧在 \texttt{LaneFollowPath} 的边界构造阶段完成威胁度计算、时变投影、扫描线分组、精确间隙生成和 Top-K 空间同伦选择；速度侧在 \texttt{SpeedBoundsDecider} 生成基础 ST 边界后构造占用补集与时间同伦图，再在 \texttt{PiecewiseJerk\allowbreak SpeedOptimizer} 的位置边界接口中按式\eqref{eq:window_to_st}融合上下界。该集成复用 Apollo 两阶段 QP 的目标函数、运动学等式和求解器，把算法增量集中在约束生成与阶段传递层。

MIKU 的关键参数沿用威胁度建模与通行带构造两节的标定值：差异化额外安全裕度取 $[0.10,0.40]$\,m，威胁度中的交互密度半径取 10\,m；扫描线分组按障碍物纵向占据区间是否重叠判定，面向城市道路中低速工况。

本文另设置 8 个确定性机制场景。P1--P4 覆盖横穿行人、动静混合障碍、窄路双侧约束和维修借道四类典型构型，用于观察关键机制在具体边界冲突中的作用；C1--C4 通过调整时间域或障碍物横向位置构造与之对应的常规可通行条件。具体构型如表\ref{tab:exp_scenarios}所示。

\begin{table}[htbp]
\centering
\caption{确定性机制场景的构型设置}
\label{tab:exp_scenarios}
\small
\begin{tabular}{c l p{7.4cm}}
\toprule
\textbf{场景对} & \textbf{构型} & \textbf{可比化调整与比较目的} \\
\midrule
P1 / C1 & 单行人横穿 & C1延长规划时域，观察动态占用下的先通过/后通过时序选择 \\
P2 / C2 & 行人加停车 & C2外移静态车辆横向中心，观察动静混合边界的分组与通行带构造 \\
P3 / C3 & 窄路加双侧交通锥 & C3将对称锥列改为轻微非对称使Baseline可通过，保留差异化裕度释放通行空间 \\
P4 / C4 & 单车道维修封闭加借道 & C4将水马横向中心调至贪心切换临界侧，观察组级选向的连续性 \\
\bottomrule
\end{tabular}
\end{table}

实验基线为固定配置下的 Apollo 分段加加速度路径--速度解耦规划器：动态障碍物主要由速度阶段处理，路径阶段不利用其到达时刻预测。MIKU 在完全相同的 QP 求解链上加入交互感知约束统一机制。随机实验还设置了时变投影加局部贪心的 B1，以及进行 3 轮到达时间更新的迭代 PVD 方法 B2，用以区分单点时变投影、反复解耦更新与 MIKU 完整约束链之间的性能差异。

本文以“轨迹到达 $s_{max}-1$\,m 且全时域无碰撞”定义规划成功，碰撞检测使用车辆与障碍物的时变真值轮廓。同时统计进度率、平均速度、jerk RMS 与完整规划调用耗时，并对同一随机种子下的方法差值进行配对 bootstrap 和 McNemar 精确检验。

\subsection{最大间隙精确求解验证}
\label{subsec:exp_exactness}

为验证引理\ref{lem:continuous_partition}与定理\ref{thm:max_gap}在程序中的一致实现，本文设置 20 个固定随机种子，每个种子生成 200 组禁行区间，共得到 4,000 个确定性测试实例。每例包含 0--8 个随机区间，区间可互相重叠、嵌套或越出道路边界；这些构型覆盖了前缀最大边界、道路裁剪与端部间隙的关键实现分支。对每个实例，分别运行本文的 $O(k\log k)$ 扫描算法与遍历 $2^k$ 绕行方向组合的穷举 oracle，并比较最优中心带宽度。

\begin{table}[htbp]
\centering
\caption{最大间隙扫描解与穷举最优解对照}
\label{tab:exactness}
\small
\begin{tabular}{cccc}
\toprule
\textbf{随机实例数} & \textbf{障碍物数 $k$} & \textbf{逐例一致数} & \textbf{最大带宽绝对误差} \\
\midrule
4,000 & 0--8 & 4,000/4,000 & 0 \\
\bottomrule
\end{tabular}
\end{table}

如表\ref{tab:exactness}所示，两种方法在 4,000 个实例上逐例一致，最大带宽绝对误差为 0。该结果直接验证了：MIKU 用 $k+1$ 个连续分割代替 $2^k$ 组合搜索时，保留了最优中心带宽度，复杂度降阶不以牺牲空间决策最优性为代价。

\subsection{配对随机场景统计验证}
\label{subsec:exp_randomized}

为检验方法对场景参数变化的适应性，本文进一步构造行人横穿、车辆切入、停车加对向来车、窄路多障碍物、交错动态障碍物、预测噪声和延迟横穿 7 个场景族。每族使用 500 个固定随机种子生成自车速度、障碍物位置、尺寸、运动速度与预测偏差，共得到 3,500 个配对场景。所有方法接收同一预测场景，轨迹使用独立保留的真值场景检测碰撞。

\begin{table}[htbp]
\centering
\caption{3,500 个配对随机场景的总体结果}
\label{tab:randomized_overall}
\small
\begin{tabular}{lrrrr}
\toprule
\textbf{指标} & \textbf{B0} & \textbf{B1} & \textbf{B2} & \textbf{MIKU} \\
\midrule
无碰撞终点到达率 (\%) & 62.7 & 52.4 & 62.1 & \textbf{77.5} \\
碰撞率 (\%) & 2.2 & 2.1 & 2.2 & \textbf{1.1} \\
平均进度率 (\%) & 77.5 & 71.1 & 76.6 & \textbf{86.4} \\
jerk RMS (m/s$^3$) & 1.252 & 1.315 & 1.300 & \textbf{1.132} \\
规划耗时 P50 (ms) & 10.08 & 11.38 & 31.20 & 9.97 \\
规划耗时 P95 (ms) & 57.26 & 63.32 & 170.53 & 69.15 \\
\bottomrule
\end{tabular}
\end{table}

表\ref{tab:randomized_overall}显示，MIKU 的无碰撞终点到达率较 B0 提高 14.8 个百分点，3,500 个配对差值的 95\% bootstrap 置信区间为 $[13.5,16.0]$ 个百分点，McNemar 精确检验 $p<10^{-132}$。同时，碰撞率下降 1.0 个百分点（$p<10^{-10}$），平均进度率提高 8.9 个百分点（95\% 置信区间 $[8.2,9.7]$），jerk RMS 下降 0.120\,m/s$^3$（95\% 置信区间 $[-0.137,-0.103]$）。MIKU 与 B0 的规划耗时中位数均为约 10\,ms，且明显低于需要三轮规划的 B2；P95 耗时为 69.15\,ms，体现了困难场景下同伦搜索和按需迭代的附加计算。总体结果证明，完整的空间--时间同伦约束链同时提升了成功率、轨迹安全性和运行平顺性，并保持了单次解耦规划器的计算效率。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_q34_quality_results}}
  \caption{无碰撞到达率与平均进度率}
  \label{fig:q34_quality}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_q34_safety_results}}
  \caption{碰撞率}
  \label{fig:q34_safety}
\end{subfigure}
\caption{3,500 个配对随机场景的总体性能对比}
\caption*{数值与表~\ref{tab:randomized_overall}一致；图中将通行能力和安全性分开呈现，便于观察 MIKU 在两个指标上的同时改善。}
\label{fig:q34_overall}
\end{figure}

为比较解耦约束统一与联合时空求解的效果，本文在每个场景族分层抽取 5 个随机种子，以 35 个场景对照 MIKU 和同时搜索时间、纵向位置、横向位置及速度状态的五维联合网格参考 B3。MIKU 与 B3 的无碰撞终点到达率分别为 85.7\% 和 82.9\%，碰撞率分别为 0 和 2.9\%；规划耗时中位数分别为 9.10\,ms 和 1,829.40\,ms，P95 耗时分别为 48.77\,ms 和 12,842.93\,ms。该对照表明，MIKU 在保留两阶段 QP 的同时，获得了与高开销联合参考相当的通行能力，中位规划耗时仅为后者的约 $1/201$。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_q34_runtime_results}}
  \caption{不同方法的 P50/P95}
  \label{fig:q34_runtime}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_q34_narrow_speed}}
  \caption{窄路场景的终点纵向位置}
  \label{fig:q34_narrow_progress}
\end{subfigure}
\caption{实时开销与典型场景通行结果}
\caption*{左图用实验表中的 P50/P95 指标呈现尾延；右图直接对比窄路场景的实验终点位置，并与后文的 SL 通行带图相互验证。}
\label{fig:q34_runtime_behavior}
\end{figure}

\subsection{确定性机制场景分析}
\label{subsec:exp_passrate}

8 个机制场景的无碰撞到达数与平均速度如表\ref{tab:exp_pass}所示。在 P1 单行人横穿中，两种方法均可以通过速度时序完成规划；在引入静态障碍、双侧约束或密集施工障碍的 P2--P4 中，基线的逐障碍物边界更新均造成通行带阻塞，MIKU 则全部无碰撞到达终点。

\begin{table}[htbp]
\centering
\caption{8 个场景的终点到达数与平均速度}
\label{tab:exp_pass}
\small
\begin{tabular}{lccc}
\toprule
\textbf{场景组} & \textbf{到达数 Baseline / MIKU} & \textbf{$\bar v$ Baseline (m/s)} & \textbf{$\bar v$ MIKU (m/s)} \\
\midrule
机制场景 P1--P4 & 1/4 \ / \ 4/4 & 2.49 & 4.67 \\
可通行场景 C1--C4 & 4/4 \ / \ 4/4 & 4.47 & 4.47 \\
\midrule
汇总 8 场景 & 5/8 \ / \ 8/8 & 3.48 & 4.57 \\
\bottomrule
\end{tabular}
\end{table}

 机制场景中，MIKU 将平均速度由 2.49\,m/s 提升至 4.67\,m/s，直接反映了连续分割、空间同伦和差异化裕度打开多障碍物通行带的效果。C1--C4 中两者均为 4/4，平均速度同为 4.47\,m/s，说明新的约束构造在常规可通行条件下保持了原规划器的速度效率。图~\ref{fig:q34_narrow_sl} 进一步展示了窄路构型中从横向通行带到纵向速度规划的差异：基线在 $s\approx16$\,m 处被阻塞，MIKU 保持可行轨迹并完成通行。大样本下的平顺性和求解效率结论则由表\ref{tab:randomized_overall}的 jerk RMS 与 P50/P95 耗时给出。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_exp_03_narrow_cones_sl_a}}
  \caption{Baseline：路径在 $s\approx16$\,m 处阻塞}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \adjustbox{max width=\linewidth}{\input{../图片/fig_exp_03_narrow_cones_sl_b}}
  \caption{MIKU：连续通行带与路径}
\end{subfigure}
\caption{窄路多障碍物场景的 SL 路径边界与通行对比}
\caption*{实验数据直接来自窄路场景的路径边界文件；基线的无可行边界和 MIKU 的连续通行带对比与表~\ref{tab:exp_pass} 的到达结果相互验证。}
\label{fig:q34_narrow_sl}
\end{figure}

\subsection{组件消融}
\label{subsec:exp_ablation}

在与主实验完全相同的 3,500 个配对场景上，本文从完整 MIKU 中每次去除一项机制，形成 A1--A7 七个减法变体。表\ref{tab:exp_ablation_score}列出全部场景的汇总结果。

\begin{table}[htbp]
\centering
\caption{3,500 个配对场景上的减法消融结果}
\label{tab:exp_ablation_score}
\small
\begin{tabular}{clrrr}
\toprule
\textbf{变体} & \textbf{删除的机制} & \textbf{成功率(\%)} & \textbf{碰撞率(\%)} & \textbf{进度率(\%)} \\
\midrule
A1 & 到达时间驱动投影 & 77.3 & 1.2 & 86.7 \\
A2 & 纵向连通分组 & 76.9 & 1.1 & 85.9 \\
A3 & Top-K 空间同伦 & 67.3 & 1.1 & 79.5 \\
A4 & 威胁度差异化裕度 & 66.7 & 1.1 & 78.9 \\
A5 & 时间同伦图 & 74.2 & 1.2 & 84.3 \\
A6 & 鲁棒预测占用管 & 77.0 & 2.2 & 86.6 \\
A7 & 交替到达时间更新 & 76.6 & 1.0 & 85.9 \\
MIKU & 完整方法 & \textbf{77.5} & \textbf{1.1} & \textbf{86.4} \\
\bottomrule
\end{tabular}
\end{table}

消融结果将各机制的作用清晰地对应到不同失效模式。去除 Top-K 空间同伦或差异化裕度后，总体成功率分别下降 10.2 和 10.7 个百分点；在窄路多障碍物场景族中，两者分别导致 62.4 和 65.4 个百分点的成功率损失，证明多候选空间选向与风险自适应边界是打开密集通行带的核心。去除时间同伦图使总体成功率下降 3.3 个百分点（$p<10^{-18}$），在延迟横穿场景族中下降 22.8 个百分点；去除鲁棒占用管则使预测噪声场景族的碰撞率上升 7.4 个百分点，并使全局碰撞率由 1.1\% 上升至 2.2\%。纵向分组和交替更新分别带来 0.5 和 0.8 个百分点的总体成功率增益，配对检验均达到统计显著；到达时间驱动投影在车辆切入场景族额外提升 2.0 个百分点。这些结果表明，MIKU 的空间容量、时序决策、预测鲁棒性与迭代协调机制形成了面向不同场景困难的互补结构。

\begin{figure}[htbp]
\centering
\adjustbox{max width=0.92\textwidth}{\input{../图片/fig_q34_ablation_results}}
\caption{3,500 个配对场景中的组件消融效应}
\caption*{条形长度表示从完整 MIKU 中删除该模块后的无碰撞到达率下降；数值与表~\ref{tab:exp_ablation_score}及配对统计一致。}
\label{fig:q34_ablation}
\end{figure}

\subsection{灵敏度分析}
\label{subsec:exp_sensitivity}

本文在 P1--P4 上对 5 个权重同时施加 $\pm20\%$ 均匀扰动并重新归一化，共取 100 组固定随机样本。相对基准的额外裕度最大偏差为 6.4\%，绝对偏差小于 0.019\,m，偏差超过 0.05\,m 的样本比例为零。P4 中出现的排序翻转集中于威胁度差小于 0.02 的同类交通锥，对应裕度差小于 0.01\,m。结果表明，差异化安全裕度在较大权重扰动下仍保持厘米级数值稳定性，近等价障碍物的次序变化不会明显改变边界尺度。

\subsection{结果讨论}
\label{subsec:exp_limitations}

配对随机实验与机制场景给出了一致结论。3,500 个随机场景中，MIKU 相对 B0 净增 517 个无碰撞成功案例，配对检验确认该改善具有统计显著性；窄路多障碍物场景族的成功率由 1.6\% 提升至 67.0\%，直接印证了连续分割、差异化裕度与空间同伦的组合作用；在预测噪声场景族中，预测占用管将碰撞率由 10.6\% 降至 3.6\%。8 个确定性机制场景进一步将总体差异定位到动静混合边界、双侧约束和密集施工障碍的具体构型。精确性对照、大样本统计、机制场景、减法消融与灵敏度由此构成从理论、实现到系统性能的完整证据链。

本文验证聚焦于结构化道路中的典型多障碍物构型。进一步研究将面向更大规模交通分布、非矩形概率预测集与实车闭环测试，持续扩展 MIKU 的场景覆盖和工程验证深度。

% ====== 5 结论 ======
\section{结论}
\label{sec:conclusion}

本文提出了面向路径--速度解耦规划器的 MIKU 多障碍物交互感知约束统一方法。MIKU 将道路、自车尺寸与障碍物裕度统一到自车中心边界语义，通过交互感知威胁度生成差异化安全裕度，以到达时间驱动的投影构造动态通行带；连续分割定理与前缀最大间隙扫描精确生成组内候选，Top-K 空间同伦进一步保证分量间的通行带连续性。路径求解后，预测不确定性占用管与占用补集生成先通过/后通过安全窗，多障碍物时间同伦图选定因果序列并将其写入 ST 位置上下边界。整个方法保持两阶段分段加加速度 QP 的求解形式不变，并在 Apollo 集成中验证了空间决策、速度时序与工程求解链的统一。

4,000 组随机区间实例上，MIKU 最大间隙扫描解与 $2^k$ 穷举最优解逐例一致，最大带宽绝对误差为 0，证明连续分割定理已准确转化为可执行算法。3,500 个配对随机场景上，MIKU 的无碰撞终点到达率较 B0 提高 14.8 个百分点，碰撞率降低 1.0 个百分点，平均进度率提高 8.9 个百分点，jerk RMS 降低 0.120\,m/s$^3$，且规划耗时中位数保持在约 10\,ms。联合时空参考对照进一步表明，MIKU 以约 $1/201$ 的中位规划耗时获得了相当的通行能力。配对置信区间和精确检验表明成功率与安全性改善均具有统计显著性。机制场景、减法消融与灵敏度结果进一步定位了时变投影、精确间隙、空间同伦、差异化裕度、预测占用管和时间同伦的作用。未来将结合更大规模交通场景、非矩形概率预测集和实车闭环测试，进一步增强 MIKU 的环境适应能力与部署验证。

\printbibliography[heading=bibintoc,title={参考文献}]

\end{document}
