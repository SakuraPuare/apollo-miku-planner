\chapter{GTOC 总体框架与问题概览}

本章建立群组感知时变通行带规划算法（Group-aware Time-varying Optimal Corridor，GTOC）的总体框架与统一数学模型，并对Apollo Baseline的次优性进行系统分析，为后续三章的具体设计提供共用的数学语言与对照基准。本章先勾勒多障碍物场景下三个核心问题的整体画像，进而建立时变最优通行带的数学模型，将多障碍物避让方向选择形式化为组合优化问题；在此模型下分析Apollo Baseline次优性在空间与时间两个维度上的根源；最后从安全距离、决策粒度、时空协调三个维度给出GTOC的整体改进路线。

\section{问题概述}

在多障碍物密集场景中，自车前方同时存在多个障碍物，例如路口多车交汇、高速多车道变道、施工区域多路障等，Apollo现有的Planning模块会面临一系列问题。图\ref{fig:problem_overview}给出了三个问题的几何示意与级联后果。子图(a)展示安全裕度一刀切导致的通行带压缩，窄小的路锥被过大的统一缓冲包裹，挤占了本应富余的横向空间。子图(b)展示独立决策矛盾导致的路径BLOCKED，左右两侧障碍物被贪心分配了相反方向的避让决策，使得$l^+(s)$与$l^-(s)$在中间区域相遇甚至翻转，可行带宽不足以容纳自车宽度。子图(c)展示动态障碍物被排除在路径阶段之外，行人的预测轨迹在Path阶段完全不可见，导致规划出的路径与速度阶段检测到的实际碰撞窗口相互矛盾。三个问题在多障碍物密集场景中沿空间、安全、时间三条维度级联作用，最终表现为不必要的FallbackPath停车和通行效率下降，这也是后续章节改进方案同时从分组决策、差异化安全裕度和时变投影三个角度切入的根本原因。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_three_problems_a}}
  \caption{安全裕度一刀切}
  \label{fig:problem_overview:a}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_three_problems_b}}
  \caption{独立决策矛盾}
  \label{fig:problem_overview:b}
\end{subfigure}

\vspace{0.6em}
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_three_problems_c}}
  \caption{动态障碍物排除}
  \label{fig:problem_overview:c}
\end{subfigure}
\caption{多障碍物场景三个核心问题的几何示意}
\label{fig:problem_overview}
\end{figure}
\section{时变最优通行带问题建模}
本节建立改进算法的数学框架，定义后续各节所需的核心符号，并在此基础上构建联合优化问题。

\subsection{SL空间中的障碍物表示}
沿用第二章介绍的Frenet坐标系（SL坐标系），纵向坐标$s$为沿参考线的弧长距离，横向坐标$l$为偏离参考线的偏移，左正右负。

对于第$i$个障碍物，其在SL空间中的投影为一个轴对齐矩形

\begin{equation}
    O_i = [s_i^{-}, s_i^{+}] \times [l_i^{-}, l_i^{+}]
    \label{eq:obs_sl_proj}
\end{equation}

其中$[s_i^{-}, s_i^{+}]$为障碍物沿参考线方向的纵向占据范围，$[l_i^{-}, l_i^{+}]$为横向占据范围。$l_i^{-}$和$l_i^{+}$分别为障碍物右侧边界和左侧边界的横向坐标，以参考线为原点，左正右负。

为保证路径规划的安全性，需要在障碍物边界外预留安全裕度。定义综合安全裕度为

\begin{equation}
    \delta = \frac{W_{ego}}{2} + d_{buf}
    \label{eq:safety_margin}
\end{equation}

其中$W_{ego}$为自车宽度，$d_{buf}$为额外的缓冲距离，对应Apollo中\texttt{obstacle\_lat\_buffer}参数，默认取0.4\,m。

基于安全裕度，定义两个关键量

\begin{align}
    u_i &= l_i^{-} - \delta \label{eq:def_u} \\
    v_i &= l_i^{+} + \delta \label{eq:def_v}
\end{align}

$u_i$表示从障碍物$O_i$右侧安全通过时，自车参考点的横向坐标上界；$v_i$表示从左侧安全通过时的横向坐标下界。换言之，若自车从右侧绕行，则需满足$l \leq u_i$；若从左侧绕行，则需满足$l \geq v_i$。两个量之间的关系为$v_i - u_i = (l_i^{+} + \delta) - (l_i^{-} - \delta) = (l_i^{+} - l_i^{-}) + 2\delta$，即障碍物宽度加两倍安全裕度，这也是障碍物在横向空间中的“有效占据宽度”。

图\ref{fig:obstacle_uvi}给出了上述定义的几何图景。障碍物$O_i$在SL空间中占据一个轴对齐矩形，横向延伸为$[l_i^-, l_i^+]$，纵向延伸为$[s_i^-, s_i^+]$。在矩形外围向左右各扩展一倍安全裕度$\delta$，形成膨胀后的“禁行矩形”。其下边界即$u_i = l_i^- - \delta$，代表从右侧绕行时自车参考点横向坐标的上限；其上边界即$v_i = l_i^+ + \delta$，代表从左侧绕行时的横向坐标下限。图中深蓝实线给出了“从右侧绕行”情形下的自车轨迹，该轨迹贴着$u_i$下方通过即可同时满足障碍物避让与道路边界约束。后续的最优通行带问题将在$u_i$和$v_i$构成的候选边界集合上做选择，因此这两个量的精确几何含义是全章推导的基石。

\begin{figure}[htbp]
\centering
\resizebox{0.8\textwidth}{!}{\input{../图片/fig_obstacle_uvi}}
\caption{障碍物安全边界 $u_i$ 与 $v_i$ 的几何定义}
\label{fig:obstacle_uvi}
\end{figure}

\subsection{避让决策与通行带}
对于在某一纵向截面$s$处活跃的每个障碍物$O_i$，即$s \in [s_i^{-}, s_i^{+}]$，需要为其分配一个避让方向决策

\begin{equation}
    d_i \in \{L, R\}
    \label{eq:decision_var}
\end{equation}

其中$d_i = R$表示从障碍物右侧即右绕通过，$d_i = L$表示从左侧即左绕通过。

每个决策对路径横向边界施加如下约束。

\begin{itemize}[noitemsep]
    \item $d_i = R$，自车应在障碍物右侧通过，构成上界约束$l^{+} \leq u_i$
    \item $d_i = L$，自车应在障碍物左侧通过，构成下界约束$l^{-} \geq v_i$
\end{itemize}

给定一组决策$\mathbf{d} = (d_1, d_2, \ldots, d_n)$，将被分配为右绕和左绕的障碍物分别记为集合$\mathcal{R} = \{i \mid d_i = R\}$和$\mathcal{L} = \{i \mid d_i = L\}$。在位置$s$处，综合所有约束后的路径横向边界为

\begin{align}
    l^{+}(\mathbf{d}) &= \min\left(l_{road}^{+},\; \min_{i \in \mathcal{R}} u_i\right) \label{eq:upper_bound} \\
    l^{-}(\mathbf{d}) &= \max\left(l_{road}^{-},\; \max_{i \in \mathcal{L}} v_i\right) \label{eq:lower_bound}
\end{align}

其中$l_{road}^{+}$和$l_{road}^{-}$分别为道路左侧和右侧边界的横向坐标。

由此定义通行带宽度

\begin{equation}
    W(\mathbf{d}) = l^{+}(\mathbf{d}) - l^{-}(\mathbf{d})
    \label{eq:band_width}
\end{equation}

当$W(\mathbf{d}) > 0$时，存在宽度为$W$的横向可通行空间；当$W(\mathbf{d}) \leq 0$时，路径被阻塞（BLOCKED），系统需要截断路径并触发降级策略。通行带越宽，路径优化器\texttt{PiecewiseJerkPathOptimizer}的横向自由度越大，输出路径的平滑性和舒适性越好。

从几何视角看，通行带$[l^{-}(\mathbf{d}), l^{+}(\mathbf{d})]$定义了自车参考点在横向上的可行区间。路径优化器在该区间内寻找最优的横向偏移曲线$l(s)$，使其满足连续性、曲率和与参考线的偏差等约束。因此，通行带的构建是路径规划的关键前置步骤，通行带的质量直接决定了后续优化问题的可行域和最优解质量。

\subsection{联合优化问题}
在真实场景中，障碍物可能处于运动状态。为此引入到达时间函数$\tau(s)$，表示自车到达纵向位置$s$的预估时刻。此时，障碍物的SL投影变为时间的函数

\begin{equation}
    O_i(\tau(s)) = [s_i^{-}(\tau(s)),\; s_i^{+}(\tau(s))] \times [l_i^{-}(\tau(s)),\; l_i^{+}(\tau(s))]
    \label{eq:time_varying_obs}
\end{equation}

相应地，$u_i$和$v_i$也成为位置$s$的函数

\begin{equation}
    u_i(s) = l_i^{-}(\tau(s)) - \delta, \quad v_i(s) = l_i^{+}(\tau(s)) + \delta
    \label{eq:time_varying_uv}
\end{equation}

这意味着障碍物对路径边界的约束不再是固定的，而是随自车到达时间而变化。一个障碍物在$t=0$时可能占据车道中心，但在$\tau(s)$时刻可能已移至道路边缘甚至完全离开参考线范围。这一时变特性是本文方法区别于Baseline的核心所在。

路径规划的目标是找到最优的避让决策$\mathbf{d}$和速度规划$\tau$，使得全路径的最小通行带宽度最大化

\begin{equation}
    (\mathbf{d}^{*}, \tau^{*}) = \arg\max_{\mathbf{d},\;\tau} \;\min_{s} \left[l^{+}(s, \mathbf{d}, \tau) - l^{-}(s, \mathbf{d}, \tau)\right]
    \label{eq:joint_opt}
\end{equation}

该联合优化问题的搜索空间为$2^n$，即$n$个障碍物各有两种决策，属于NP-hard问题，暴力枚举不可行。搜索空间随$n$呈指数增长，在Apollo的100\,ms规划周期约束下无法满足实时性要求。

然而，该问题具有特殊的几何结构，决策变量$d_i$只影响一维横向空间中的边界位置，且影响方式是单调的，一个方向收缩上界，另一个方向收缩下界。下文将利用这一结构，通过引理和定理的推导，将求解复杂度降至$O(n \log n)$。

\section{Apollo Baseline的次优性分析}

\subsection{Baseline的贪心策略}
Apollo通过\texttt{UpdatePathBoundaryBySLPolygon}函数逐障碍物构建路径边界。该函数在路径规划的每个离散位置$s$处被调用，是PathBoundsDecider中处理障碍物避让的核心入口。其核心逻辑可形式化描述如下。

初始化路径边界为道路边界，$l^{+}(s) = l_{road}^{+}(s)$，$l^{-}(s) = l_{road}^{-}(s)$。

对于每个在位置$s$处活跃的障碍物$O_i$，独立决定避让方向

\begin{equation}
    d_i = \begin{cases}
        R & \text{若} \; \dfrac{l_i^{-} + l_i^{+}}{2} > l_{last\_nudge} \\[0.8em]
        L & \text{其他}
    \end{cases}
    \label{eq:baseline_decision}
\end{equation}

其中$l_{last\_nudge}$为上一次nudge操作记录的横向位置参考值。若仅一侧可通行，则该障碍物的避让方向直接赋为可通行侧，与4.3.2节代码所示逻辑一致。然后更新边界。

\begin{itemize}[noitemsep]
    \item $d_i = R$，$l^{+}(s) \leftarrow \min(l^{+}(s),\; u_i)$
    \item $d_i = L$，$l^{-}(s) \leftarrow \max(l^{-}(s),\; v_i)$
\end{itemize}

若更新后$l^{+}(s) < l^{-}(s)$，则判定该位置BLOCKED，调用\texttt{TrimPathBounds}截断路径，最终触发FallbackPath停车。

该策略的关键特征是贪心性和局部性。所谓贪心性，是指对第$i$个障碍物的决策仅基于该障碍物自身的位置信息，不考虑已做出的其他决策是否与之矛盾。所谓局部性，是指决策仅使用当前时刻的空间快照，不利用Prediction模块提供的未来轨迹预测。

具体而言，该策略存在两方面的次优性。其一，避让方向的决策仅依赖障碍物中心与参考值的相对位置，逐个独立进行，未考虑多个障碍物的全局协调；其二，障碍物投影仅使用$t=0$时刻的快照，动态障碍物通过\texttt{IsWithinPathDeciderScopeObstacle}被直接排除在路径决策之外。Apollo的开发团队在该过滤函数的源码中留有\texttt{TODO}注释，明确提到应当在未来版本中将动态障碍物纳入路径决策，这从侧面印证了该设计的局限性。图\ref{fig:baseline_greedy_flow}给出了该贪心流程的完整控制流，从初始化路径边界到逐障碍物决策，再到BLOCKED判定与回退停车的全链路。

\begin{figure}[htbp]
\centering
\resizebox{0.8\textwidth}{!}{\input{../图片/fig_baseline_greedy_flow}}
\caption{Apollo Baseline逐障碍物贪心决策的控制流}
\label{fig:baseline_greedy_flow}
\end{figure}

\subsection{空间维度的次优性}

空间维度的次优性集中表现为矛盾决策现象。以下用一个对称场景说明。

\textbf{场景设定}\quad 双车道道路总宽7.5\,m，$l_{road}^{-} = -3.75$\,m，$l_{road}^{+} = 3.75$\,m。在某纵向截面$s^{*}$处同时存在两个路锥，关于参考线近似对称

\begin{itemize}[noitemsep]
    \item $O_a$偏右，横向占据$[0.1,\; 0.4]$\,m，中心$0.25$\,m
    \item $O_b$偏左，横向占据$[-0.4,\; -0.1]$\,m，中心$-0.25$\,m
\end{itemize}

安全裕度$\delta = 1.2$\,m，对应$u_a = 0.1 - 1.2 = -1.1$\,m，$v_a = 0.4 + 1.2 = 1.6$\,m，$u_b = -0.4 - 1.2 = -1.6$\,m，$v_b = -0.1 + 1.2 = 1.1$\,m。

\textbf{Baseline贪心决策}\quad $O_a$中心$0.25 > 0$，分配$d_a = R$；$O_b$中心$-0.25 < 0$，分配$d_b = L$。路径边界从两侧同时收缩

\begin{equation}
    l^{+}(s^{*}) = u_a = -1.1\,\text{m}, \quad l^{-}(s^{*}) = v_b = 1.1\,\text{m}
\end{equation}

$l^{+} < l^{-}$，判定BLOCKED。贪心策略将自车挤入两个路锥之间仅0.2\,m的间隙，路径不可行。

\textbf{全局最优决策}\quad 按$u_i$升序排列为$O_b, O_a$，此时$u_b = -1.6 < u_a = -1.1$，计算3个间隙

\begin{align}
    g_0 &= u_b - l_{road}^{-} = -1.6 - (-3.75) = 2.15\,\text{m} \notag \\
    g_1 &= u_a - v_b = -1.1 - 1.1 = -2.2\,\text{m} \quad (\text{负值，中间通道不可行}) \notag \\
    g_2 &= l_{road}^{+} - v_a = 3.75 - 1.6 = 2.15\,\text{m} \notag
\end{align}

最大间隙$g_0 = g_2 = 2.15$\,m，对应分割点$p^{*} = 0$即统一RIGHT\_NUDGE或$p^{*} = 2$即统一LEFT\_NUDGE。以$p^{*} = 0$为例，通行带为$[l_{road}^{-},\; u_b] = [-3.75,\; -1.6]$\,m，宽度2.15\,m，自车可借用相邻车道安全通过。

贪心策略在$2^2 = 4$种方向组合中恰好选中了唯一不可行的一种（$R, L$），而全局搜索只需检查$k+1 = 3$个间隙即可找到最优解。当所有间隙均为负值时，说明本车道确实不存在可通行路径，此时算法输出的BLOCKED是真实的而非贪心策略导致的虚假阻塞，体现了算法的完备性。

图\ref{fig:greedy_vs_global}直观对比了贪心决策与全局协调在该场景下的差异。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_greedy_vs_global_a}}
  \caption{贪心独立决策 $\to$ BLOCKED}
  \label{fig:greedy_vs_global:a}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_greedy_vs_global_b}}
  \caption{全局协调统一右绕 $\to$ 通行}
  \label{fig:greedy_vs_global:b}
\end{subfigure}
\caption{贪心独立决策与全局协调决策的通行带对比}
\label{fig:greedy_vs_global}
\end{figure}

\subsection{时间维度的次优性}
Baseline的另一个局限在于仅使用$t = 0$时刻的障碍物位置快照构建路径边界，完全忽略了动态障碍物的运动趋势。以横穿行人场景为例，行人在$t = 0$时刻占据车道中心，Baseline将其排除在路径决策之外，但仍会在ST图中标记为障碍物，导致速度规划层进行不必要的减速甚至停车。事实上，自车到达行人所在纵向位置时行人通常已横穿至车道一侧，车道中心的间隙已经打开，原本无需停车。时变通行带方法通过预测轨迹自然捕获这一时间维度的信息。图\ref{fig:time_varying_gap}直观对比了两种方法在该场景下的差异。

\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_time_varying_gap_a}}
  \caption{Baseline 仅看 $t{=}0$}
  \label{fig:time_varying_gap:a}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.48\textwidth}
  \centering
  \resizebox{\linewidth}{!}{\input{../图片/fig_time_varying_gap_b}}
  \caption{时变方法利用预测轨迹}
  \label{fig:time_varying_gap:b}
\end{subfigure}
\caption{Baseline静态快照与时变方法的通行带对比}
\label{fig:time_varying_gap}
\end{figure}

此外，Baseline还存在安全裕度维度的次优性，所有参与路径决策的障碍物使用统一的安全裕度$\delta$。然而，不同障碍物对自车的威胁程度差异显著，一个远离车道的路锥与一辆正对车道的违停车所需的安全裕度不应相同。统一的$\delta$导致低威胁障碍物占用了过多的横向空间，使通行带被不必要地压缩。

上述三类次优性的共同根源在于Baseline缺乏全局视角和分级机制，空间维度上对各障碍物独立决策，时间维度上仅使用静态快照，安全裕度维度上一刀切处理。本文提出的改进方法分别从分组即全局最优决策、时变即预测轨迹利用和分级即差异化安全裕度三个维度同时解决上述问题。

\section{GTOC 三层改进路线图}
\label{sec:gtoc_roadmap}

表\ref{tab:problem_summary}对比了三个问题的特征及其对应的改进方向。

\begin{table}[htbp]
\centering
\caption{多障碍物场景三个核心问题与改进方向}
\label{tab:problem_summary}
\small
\begin{tabularx}{\textwidth}{lXXX}
\toprule
& \textbf{问题一\quad 安全裕度一刀切} & \textbf{问题二\quad 独立决策} & \textbf{问题三\quad 动态障碍物排除} \\
\midrule
\textbf{涉及模块} & PathBoundsDecider & PathBoundsDecider & PathBoundsDecider / PathDecider \\
\textbf{根源} & 统一安全距离不区分威胁 & 逐障碍物贪心决策 & 动态障碍物不参与路径决策 \\
\textbf{表现} & 通行带被不必要压缩 & 路径BLOCKED $\rightarrow$ 停车 & 错过时变可行方案 \\
\textbf{严重程度} & 严重，加剧问题二 & 致命 & 严重，加剧问题二 \\
\textbf{改进维度} & 差异化安全距离 & 全局协调决策 & 时空约束协调 \\
\textbf{对应章节} & 第五章 & 第六章 & 第七章 \\
\bottomrule
\end{tabularx}
\end{table}

GTOC在前述统一数学模型之下，沿安全距离、决策粒度、时空协调三个维度展开三项改进。三项改进各自针对一个核心问题独立可读，但在数学框架内共享通行带宽度、安全裕度、到达时间等核心量。

\begin{enumerate}
\item \textbf{改进点一\quad{}多因子威胁度评估与差异化安全距离}\quad{}从源头释放被一刀切缓冲压缩的通行空间，对应第五章。
\item \textbf{改进点二\quad{}组级最优通行带与最大间隙策略}\quad{}从决策粒度层面消解逐障碍物贪心导致的矛盾避让方向，对应第六章。
\item \textbf{改进点三\quad{}时空可通行走廊与路径-速度协调}\quad{}将动态障碍物纳入路径决策并建立路径-速度约束传递通道，对应第七章。
\end{enumerate}

三项改进在数学层面前后衔接：改进点一产出的差异化安全裕度$\delta_i$进入改进点二的最大间隙公式，改进点三在ST层进一步利用改进点二的连通分量分组结果与改进点一的威胁度排序。第八章给出GTOC集成至Apollo系统的工程方案与综合仿真验证。

\section{本章小结}

本章建立了GTOC的总体框架。先从安全距离、决策粒度和时空协调三个维度勾勒了多障碍物场景下三个核心问题的整体画像，进而建立时变最优通行带的统一数学模型，将多障碍物避让方向选择形式化为组合优化问题，并在该模型下分析了Apollo Baseline次优性在空间与时间两个维度的具体来源；最后从三个维度给出GTOC的整体改进路线，明确三项改进在数学层面的衔接关系。后续三章在该共用框架之下，分别展开三项改进的具体设计。
